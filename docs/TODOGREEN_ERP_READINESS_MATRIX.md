# Matriz de prontidão do ERP To Do Green

**Revisada em 24/09/2026** contra o código do PR #15 — a `main` até `f856e9f`
mais as mudanças do próprio PR; em 25/09, a linha do Planner foi atualizada
com a `main` até `f6b9548` (compartilhamento de plano e menu por pacotes).
Cada linha foi reconferida contra o código, e todo achado novo de controle foi
reproduzido por requisição HTTP contra o Worker local antes de entrar em
[Lacunas de controle](#lacunas-de-controle); o que só a leitura do código
mostrou está separado, em
[Achados de leitura](#achados-de-leitura-ainda-não-reproduzidos). A versão
anterior (aberta em 30/08 e "revalidada em 13/09") não refletia as mudanças de
18 a 23/09 e se contradizia em vários pontos — a lista do que mudou está em
[Divergências corrigidas nesta revisão](#divergências-corrigidas-nesta-revisão).

Esta matriz não mede se existe uma tela. Para cada processo, ela responde a três
perguntas **independentes** — um processo pode estar implementado e testado e,
mesmo assim, nunca ter sido homologado em produção:

| Dimensão | Pergunta | Valores |
| --- | --- | --- |
| **Implementado** | O efeito de negócio existe no servidor — persistência no D1, regra server-side, controle de acesso (e tela, quando o processo tem tela)? | **Sim** · **Parcial** (núcleo real com lacuna declarada) · **Preparado** (código pronto e dormente até credencial, certificado ou infraestrutura externa) · **Não** (só catálogo, simulação, domínio sem consumidor ou nada) |
| **Testado** | Há teste automatizado **específico** do comportamento, rodando no gate (`npm run verify`: unidade + worker; ou `npm run test:e2e:critical`)? | **Sim** · **Parcial** (só parte do comportamento, ou só o domínio puro) · **Não** · **n/a** |
| **Homologado em produção** | Há **registro escrito** de validação com dado ou credencial real em produção (`orianone.app`, Worker `allgreen`)? | **Registrado** (com o código da evidência, H1–H7) · **Parcial** (a evidência cobre só parte — ex.: autenticação HTTP, sem dado real) · **Não registrado** · **n/a** (não há o que validar em produção, como a guarda de tokens visuais) |

"Não registrado" não quer dizer que não funcione em produção: quer dizer que
ninguém registrou a validação. Homologação nunca é inferida de "está no ar", de
teste local ou de publicação bem-sucedida.

**Resumo desta revisão:** nenhum processo de negócio tem homologação registrada
com dados reais. As evidências escritas cobrem publicação e smoke HTTP (H1, H2,
H7), a autenticação HTTP dos webhooks do TRACK3R (H3), a resolução de papéis num
deploy paralelo (H4) e o login Google antes da separação All Green (H5); o
antigo H6 foi retirado, porque não é registro de validação. Doze controles
tinham lacuna: três foram corrigidos neste PR (L4, L5, L7) e nove seguem
abertos, cinco deles achados nesta segunda passada (L8–L12). Há ainda um
defeito que quebra o faturamento a partir da segunda fatura do mesmo tipo no
espaço ([D1](#defeitos)). Em 24/09 a produção passou o dia sem rodar a `main`
e só a alcançou às 23:53 UTC, pelo botão manual do GitHub Actions — ver
[Estado observado da publicação](#estado-observado-da-publicação-não-é-homologação).

## Registro de homologação em produção

Toda validação com dado ou credencial real em produção entra aqui, com data,
ambiente e o que foi conferido. Linha nova no mesmo PR que muda o status.

| Código | Data | Evidência escrita | O que prova — e o que não prova |
| --- | --- | --- | --- |
| **H1** | 13/09/2026 | `docs/DEPLOYMENT_RUNBOOK.md` §13a: publicações com smoke HTTP (`/api/status`, SPA 200, `/api/todogreen/records` 401, `/api/system/version` com SHA e migrações esperadas) | Publicação, migrações aplicadas e rotas vivas. Não prova processo com dado real. Anterior à separação All Green (20/09), com o D1 da época (`seu-funcionario-db`). |
| **H2** | 19–20/09/2026 | `docs/HOMOLOGACAO_GO_LIVE.md` (versão em produção conferida por `/api/status`, sem SHA nem Worker identificado) | Que a produção de então respondia `/api/status` operacional. O "deploy do Workers Builds" citado no documento é a prévia de um PR do repositório anterior, ainda por mergear — não uma publicação. Anterior à separação All Green. O mesmo documento declara pendentes "homologar com dados/volume reais" e o backup/restore D1/R2. |
| **H3** | 21/09/2026 (registrado em 22/09) | Commit `3fa3876` — a mensagem e o PENDENCIAS_DA_TITULAR.md do diff (o arquivo foi removido em `0bdd1fe`): 11 segredos individuais dos webhooks TRACK3R cadastrados no Worker `allgreen` e conferidos no ar (200/401/503) | Só a autenticação HTTP de cada endpoint, sem evento real do fornecedor. O código tem 13 webhooks; os dois ainda sem segredo próprio (cotações e faturas de rede terceira) responderam 503. |
| **H4** | 20/09/2026 | Commit `913f4ef` ("homologação por perfil"; o doc de apoio foi removido em `0bdd1fe` e existe só no histórico) | Papéis admin, financeiro, motorista, colaborador e vendedor resolvidos corretamente num deploy **paralelo** (`allgreen.brunapsiles.workers.dev`), com contas de teste cujos vínculos foram inseridos direto no banco; só `GET /api/todogreen/access` foi conferido. Prova acesso, não processo — e não exercita pedido, convite nem senha provisória. |
| **H5** | 17/07/2026 | `AGENTS.md`, "Pendências conhecidas": "login e envio pelo navegador (Gmail send/compose e Agenda, via token do GIS) validados em 17/07/2026, antes da separação All Green/`orianone.app`" | Anterior ao domínio `orianone.app` e ao Worker atual. |
| **H6** | — | *Retirado nesta revisão.* A versão anterior citava o `AGENTS.md` sobre verificação e recuperação por código via Brevo, que só descreve implementação e segredos no cofre — sem data nem entrega real conferida | Não é registro de validação. A linha "E-mail transacional (Brevo)" passou a "Não registrado". |
| **H7** | 13/09/2026 | `docs/CLOUDFLARE_BUILDS_SETUP.md`: consolidação que levou `d2396e44d8e4` a produção; `0119` aplicada no D1 de produção | Publicação e schema, anteriores à separação All Green (Worker `seufuncionario-expo`, D1 `seu-funcionario-db`); a data da aplicação da 0119 não está registrada. A publicação é a mesma da primeira linha do H1. |

### Estado observado da publicação (não é homologação)

O que a produção responde publicamente. Serve para saber **qual código está no
ar** — não prova que um processo funciona com dado real.

| Quando | `GET /api/system/version` | Leitura |
| --- | --- | --- |
| 24/09/2026 19:18 UTC | `sha 6399f4a49f18`, build 14:09 UTC, `publishedBy: manual`, `branch: publish-tickets`, última migração `0144_client_requests_operation` | Publicação **manual**: o build não rodou no Workers Builds nem no GitHub Actions (`vite.config.js` só grava o provedor de CI quando há um), e sim a partir de um ramo local. A `main` estava em `49adc35` — o merge do PR #14 às 14:17 UTC ainda não estava no ar cinco horas depois, então **a produção não era a `main`**. O `/api/status` ainda devolvia o bloco `roadmap` fixo que este PR remove. |
| 24/09/2026 20:19 UTC | a mesma resposta (`6399f4a49f18`, `manual`, `publish-tickets`, `0144`) | A `main` já estava em `13ac6f8`: a produção seguia sem os PRs #14 e #16 — entre outras coisas, a extensão instalável, o anti-robô e a migração `0145`. |
| 25/09/2026 00:14 UTC | `sha f6b9548f6c84`, build 23:50 UTC de 24/09, `publishedBy: github-actions`, `branch: main`, última migração `0145_busca_vetores` | A produção passou a ser a `main` (merge do PR #21, com os PRs #14, #16, #17 e #19), publicada pelo botão manual "Publicar" do GitHub Actions (`deploy.yml`, `workflow_dispatch`, execução 3, 23:41–23:53 UTC); a execução 2 tinha publicado `f856e9f` (PR #17) às 23:30. O Workers Builds continua sem publicar sozinho. Este PR ainda não está no ar: o `/api/status` de produção segue com o bloco `roadmap` fixo. No mesmo `/api/status`, `antiRobo.configured` e `aiGateway.configured` vêm `false`: o Turnstile e o AI Gateway estão publicados, mas inertes, sem as chaves. |

## Comercial, cliente e receita

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Cadastro de cliente / Conta 360 | Sim | Sim | Não registrado | `todogreen_clients` (0032) e `todogreen_client_assignments` (0039); `handleTodoGreenClients` (`todogreen-clients.js`) com `revision` (409 na revisão velha), auditoria e importação idempotente. **Fronteira de política:** o PATCH exige só o alcance da carteira, não permissão de escrita, e a carteira inteira vem pelo papel para owner, admin, liderança comercial, pricing, financeiro, operações e sustentabilidade (`podeVerTodaCarteira`) — que assim editam o CRM e ligam ou desligam o portal de qualquer cliente. Testes: `todogreen-client-assignments.worker.test.js` ("não entrega clientes sem atribuição ao vendedor", "somente pessoa habilitada define a carteira", "cada vendedor enxerga somente os próprios clientes", "vendedor atualiza a visão 360º somente da própria carteira e com revisão", "administrador mantém visão completa e pode retirar atribuição"), `todogreen-customer-portal.worker.test.js` ("PATCH liga/desliga o portal e o GET lista os usuários do cliente"), `ClientsPage.test.jsx` ("explica e exibe somente a carteira devolvida para o vendedor", "conecta conta, forecast e próxima melhor ação") e o passo 1 da jornada. |
| CRM / carteira | Sim | Sim | Não registrado | Inteligência em `fields_json`, normalizada no servidor por `crm-fields.js` e lida na tela por `todoGreenCrmDomain.js`; comentários (0078) e interações (0079) com autor da sessão; recorte de carteira no servidor. Testes: `todogreen-vertical-records.worker.test.js` ("grava a ata da reunião com autor da sessão e carimba a conta e a oportunidade", "tentativa de contato é registro de primeira classe", "o espaço de outra pessoa não vê as interações", "grava comentário de conta e de oportunidade, com autor da sessão", "lista so o que esta na propria carteira", "a gestora continua vendo a carteira inteira"), `interacoesDomain.test.js` ("dentro da oportunidade, vê a dela e a da conta — nunca a de outra oportunidade"), `OpportunitiesPage.test.jsx` ("comentário da conta aparece na oportunidade; o novo fica só nela"). Até 24/09 os comentários e as interações salvos não apareciam em Clientes, Oportunidades e Avanços da semana; corrigido neste PR, teste `dadosDaVertical.test.js` ("repassa comentários e interações que os registros carregaram"). LinkedIn oficial não integrado — só busca pública `site:linkedin.com`. |
| Oportunidade | Sim | Sim | Não registrado | `todogreen_opportunities` (0041, título na 0081); o cliente é opcional (lead sem conta), mas, quando informado, precisa existir no espaço; auditoria. Testes: `todogreen-vertical-records.worker.test.js` ("cria, lista e devolve o que foi gravado", "o espaço de outra pessoa não aparece na lista", "a segunda gravação em cima da mesma versão é recusada", "registro de outro espaço responde 404, não 403", "recusa oportunidade que aponta um cliente inexistente (404)"), `OpportunitiesPage.test.jsx` ("atualiza o estudo preservando a revisão concorrente") e o passo 2 da jornada. |
| Handoff de oportunidade ganha | Sim | Sim | Não registrado | `deveCriarHandoff` (`efeitos-do-ganho.js`) → item na Central (`todogreen_work_items`) e implantação (`todogreen_implementation_projects`), idempotente. **Fronteira:** o efeito só roda no PATCH; oportunidade criada já "Fechada ganha" por POST — como faz a importação do pipeline com projeto ganho — não abre handoff nem implantação. Testes: `todogreen-vertical-records.worker.test.js` ("cria um único item na Central de Trabalho", "abre a implantação que a tela Implantação lê, quando há cliente vinculado", "sem cliente vinculado não cria implantação — mas ainda registra o handoff", "remarcar como ganha não duplica a implantação (idempotente)"), `opportunityHandoff.test.js` ("abre o handoff somente na transição para fechada ganha"). |
| Precificação | Parcial | Sim | Não registrado | O motor existe no servidor (`POST /api/todogreen/simulate`, `pricing_scenarios` 0027) com a régua versionada (0037, 0060 e `0086_todogreen_pricing_parameters_workspace.sql`; `todogreen-pricing-parameters.js`), mas **nenhuma tela o chama**: a Precificação calcula no navegador e grava por `/api/todogreen/records/scenarios`, que guarda resultado, aprovações e premissas vindos do corpo ([L11](#lacunas-de-controle)). Salvar pelo `/simulate` exige `pricing:simulate` ([L7](#lacunas-de-controle), corrigida). Testes: `todogreen-simulate.worker.test.js` ("quem não simula preço não salva cenário", "quem simula preço salva o cenário"), `todogreen-pricing-parameters.worker.test.js` ("vendedor não muda", "sem justificativa, nem o gestor muda", "régua que produziria preço irreal é barrada", "a régua nova passa a valer e a anterior fica encerrada, não apagada"), `todogreen-vertical-records.worker.test.js` ("salva com a procedência das premissas junto", "simulação sem resultado calculado não entra", "simulação salva não pode ser editada"), `pricingParametersDomain.test.js` ("valor fora da faixa é recusado com a faixa no aviso"). O passo 3 da jornada usa o `/simulate`, caminho que nenhuma tela usa. |
| Deal Desk | Parcial | Sim | Não registrado | Alçada, segregação solicitante/decisor, histórico e versão no servidor (0042, `todogreen-deal-desk.js`); o gate `proposalLiberada` (`vertical-records/gates.js`) barra a proposta com pedido pendente, recusado, vencido ou cancelado. **Fronteira:** o pedido é opcional — sem pedido, a proposta sai mesmo com `approval.required` no cenário ([O1](#achados-de-leitura-ainda-não-reproduzidos)) —, e a alçada sai do resultado gravado, que pelo caminho da tela veio do navegador (L11). Testes: `todogreen-deal-desk.worker.test.js` ("a alçada, o desvio e o prazo saem do resultado gravado, não do corpo do pedido", "quem pede não decide o próprio pedido, nem chamando a API direto", "papel abaixo da alçada não decide", "revisar sobe a versão, limpa a decisão e volta para pendente", "PATCH rascunho→enviada com pedido pendente é bloqueado (409)"), `todogreen-vertical-records.worker.test.js` ("pedido pendente para a simulação bloqueia a proposta direto no servidor", "pedido aprovado libera a proposta"), `DealDeskPage.test.jsx` ("quem pediu não vê botão de decidir, e a tela diz por quê"). |
| Proposta | Parcial | Sim | Não registrado | O cenário é obrigatório, mas só se confere que o `cenarioId` veio preenchido — não que exista no espaço nem que seja do cliente ([L12](#lacunas-de-controle)). Os gates do Deal Desk e da viabilidade rodam na criação e no PATCH; o da viabilidade só vale para proposta ligada a oportunidade. A proposta pode nascer já aceita (`situacao` é texto livre); o aceite do cliente é um estado informado ao ERP, não um aceite eletrônico. Testes: `todogreen-vertical-records.worker.test.js` ("sem cenário, não salva", "com cenário, salva"), `todogreen-viability.worker.test.js` ("liberar (sent) sem snapshot é recusado no servidor", "snapshot com falta continua bloqueando e nomeia o que falta", "PATCH de rascunho para liberada passa pelo mesmo gate", "proposta sem oportunidade continua só com o gate do Deal Desk") e o passo 5 da jornada. |
| Contrato / versionamento | Sim | Sim | Não registrado | Nasce de proposta aceita e cliente coerente: `conferirParDoContrato` roda na criação e no PATCH que troca proposta ou cliente ([L5](#lacunas-de-controle), corrigida). Cada alteração soma `version` e grava evento (0048/0052). O valor do contrato é digitado: o preenchimento a partir do cenário é sugestão, e o servidor não compara com o cenário aprovado. Testes: `todogreen-vertical-records.worker.test.js` ("preserva cliente, oportunidade e simulação e impede duplicidade", "versiona alterações e preserva a trilha do ciclo contratual", "o PATCH repete a checagem da criação quando proposta ou cliente mudam") e o passo 7 da jornada. |
| Jurídico no ciclo contratual | Parcial | Sim | Não registrado | O gate existe: `juridicoConcluido` (`vertical-records/gates.js`) exige documento em `todogreen_legal_records` (0090) aprovado ou assinado, na criação e na transição do contrato; como legado, aceita também um fluxo `legal` do painel empresarial (0076) com só a etapa `juridico` aprovada — é o caminho da jornada. **Lacunas:** o status do documento jurídico pode ser gravado direto ([L1](#lacunas-de-controle)), e a etapa `juridico` do legado é decidida por quem tem `deal:review`, não `compliance:manage` ([O2](#achados-de-leitura-ainda-não-reproduzidos)). A trava da criação (contrato já aprovado ou assinado no POST) não tem teste. Testes: `todogreen-vertical-records.worker.test.js` ("conclui o gate pela página do Jurídico (sistema unificado), não só pelo fluxo empresarial") — que cria o documento já `aprovado`, o próprio atalho da L1 —, `legalWorkflowDomain.test.js` ("não-Jurídico não valida nem reprova", "não deixa pular etapa (aprovar direto do rascunho)") e a jornada (409 antes do Jurídico). |
| Assinatura contratual | Parcial | Parcial | Não registrado | `signed` exige anexo no cofre com contexto jurídico (`documentoDeAssinaturaVinculado`: `legal` no documento jurídico ou, legado, `workflow` no fluxo empresarial). **Lacunas:** o gate confere a existência do anexo, não o conteúdo nem a assinatura ([L2](#lacunas-de-controle)); e nenhuma tela montada cria esse anexo — o botão "Confirmar assinatura" só passa com o anexo criado direto pela API ([O3](#achados-de-leitura-ainda-não-reproduzidos)). Nos testes o anexo é inserido por SQL. A validade jurídica do provedor/certificado é externa. Testes: `todogreen-vertical-records.worker.test.js` ("versiona alterações e preserva a trilha do ciclo contratual") e a jornada (409 da assinatura sem anexo). |
| Provedor de assinatura | Não | n/a | Não registrado | Não há integração com provedor (ClickSign, DocuSign, ZapSign, D4Sign…); o único readiness é `contractIsCurrent` (`clientActivationDomain.js`), que exige contrato assinado e aprovado. |
| Tabela de preço | Sim | Parcial | Não registrado | Cadastro mestre `price-tables` e `price-rows` (`todogreen-master-data.js`, 0062), com escrita por `pricing:manage`, `proposal:manage` ou `finance:manage`, na tela Cadastros (`masterRegistryConfig.js`). O contrato aponta a tabela e o go-live exige que ela exista, esteja ativa e seja do cliente/contrato. As linhas de preço não têm teste; a tabela só aparece no passo 6 de `todogreen-erp-journey.worker.test.js` ("atravessa Comercial, Jurídico, Implantação, Operação, Fiscal e Financeiro pela API"). |
| Implantação / go-live | Sim | Sim | Não registrado | O readiness exige tabela de preço existente, ativa e do cliente/contrato (`todogreen-client-activation.js`, 0059/0062) e operação confirmada. Tabela inexistente ou de outro cliente não tem teste — só a inativa, na jornada. Testes: `clientActivationDomain.test.js` ("sem operação, o gate pede o cadastro real e não fabrica nada", "operação em RASCUNHO não libera: exige confirmação", "operação confirmada (fora do rascunho) libera o gate"), `todogreen-client-briefing.worker.test.js` ("NÃO bloqueia a implantação: as duas leituras convivem") e os passos 11 e 12 da jornada. |
| Portal do cliente | Parcial | Sim | Não registrado | Escopo vem da sessão e do banco (`clientScopeForSession`, `customer-portal/sessao.js`); operações, solicitações (0040; por encomenda na 0144, com operação e fase reconferidas no servidor), documentos, faturas, relatório, Green Score, NPS (0115) e central de atendimento, cujo assistente recusa a pergunta que tenta mandar na IA (Prompt Guard). Nas operações, o que sai para o cliente é lista fechada. **Lacuna:** as solicitações ainda enviam `fields_json` inteiro — hoje `registradaPor` (e-mail interno), `canalDeOrigem`, `origem` e `triagemMotivo` — e `abertaPor` mostra o e-mail da equipe quando é ela quem registra; o contrato só trava as chaves, não o conteúdo de `campos`. Ligar ou desligar o portal de um cliente vale para quem o alcança na carteira, sem exigir gestão. Testes: `todogreen-customer-portal.worker.test.js` ("as operações listadas são só as dele", "pedir o outro cliente na URL não muda nada — o parâmetro não existe", "campo livre interno (margem, custo, CPF) não vaza no payload do portal", "perde o acesso na hora, sem precisar mexer no vínculo", "nenhuma permissão interna chega ao cliente", "o cliente B não abre solicitação numa encomenda do cliente A", "pergunta que tenta mandar na IA recebe a mesma recusa (Prompt Guard)"), `todogreen-portal-operacoes.worker.test.js` ("operação de outro cliente responde 404"), `todogreen-portal-multiempresa.worker.test.js` ("empresa fora do vínculo é recusada, mesmo existindo"), `todogreen-portal-nps.worker.test.js` ("a nota de A não aparece na leitura de B, nem com parâmetro forjado na URL"), `todogreen-portal-contrato.worker.test.js` ("a operação chega pela rota só com os campos livres da whitelist"), `customerPortalViewContract.test.js` ("tem exatamente as chaves do contrato") e o e2e crítico `todogreen-portais-auth.spec.js` ("cliente vinculado entra no próprio portal com a sessão real"). |
| Prévia interna do Portal do Cliente | Sim | Não | Não registrado | `customer-portal/previa.js` (`GET /api/todogreen/client-portal-preview/:cliente`), aberta pela Conta 360: monta o escopo e o menu do portal com o papel escolhido, só leitura, sem sessão de cliente nem trilha; fora da carteira responde 404 para quem não tem `clients:manage`. Sem teste. |
| E-mail ao cliente pelo CRM | Sim | Parcial | Não registrado | `handleTodoGreenSendEmail` (`POST /api/todogreen/send-email`, `todogreen-clients.js`) envia pelo Brevo e grava o contato no cliente, dentro da carteira. O envio bem-sucedido e a gravação do contato não são exercitados. Testes: `todogreen-send-email.worker.test.js` ("exige sessão", "recusa e-mail de destino inválido", "exige assunto e mensagem", "sem credencial de e-mail no cofre, avisa em vez de falhar", "motorista não usa o envio do espaço"). |
| Importação de carteira e pipeline | Sim | Parcial | Não registrado | Contas por `POST /api/todogreen/clients/import` (idempotente); oportunidades e interações do quadro externo entram uma a uma pela esteira normal, orquestradas no navegador (`ImportarPipelineModal.jsx`, `pipelineImportDomain.js`). Projeto ganho entra já "Fechada ganha" por POST e não abre handoff. Testes: `todogreen-client-assignments.worker.test.js` ("importa um lote de forma idempotente, classifica a temperatura e atribui à sessão"), `ImportarPipelineModal.test.jsx` ("grava contas, oportunidades e interações com o vínculo certo", "reimportar não duplica o que já está registrado"). |
| Linha do tempo e saúde da conta | Parcial | Parcial | Não registrado | `todogreen-timeline.js` (`GET /api/todogreen/timeline/:cliente`) monta a história da conta com as tabelas de evento que já existem, no recorte de carteira, e devolve saúde, white space, share of wallet e próxima melhor ação (`accountTimelineDomain.js`, `accountHealthDomain.js`). Nenhuma tela chama o endpoint. Só teste de domínio: `accountTimelineDomain.test.js` ("evento sem data não entra", "evento sem tipo conhecido ou sem título não entra"), `accountHealthDomain.test.js` ("os pesos somam 100 e ficam visíveis", "todo fator explica a própria nota"). |

## Operação, frota e execução

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Ordem de Serviço | Sim | Sim | Não registrado | Exige contrato aprovado e assinado e implantação ativa — na API interna, na entrada TMS e na OS faturável da entrega; preço herdado (`precoUnitarioDaOs`) e máquina de estados (`transactionalSpineDomain.js`, aplicada em `todogreen-transactions.js`; 0056). A recusa por contrato não assinado só tem teste na entrada TMS. Testes: `todogreen-transactions.worker.test.js` ("cria uma ordem somente sobre contrato aprovado e assinado", "herda o valor MENSAL do contrato como valor fechado (não multiplica pela quantidade)", "contrato marcado por unidade multiplica o preço pela quantidade", "o preço digitado vence a herança"), `todogreen-tms-manual.worker.test.js` ("sem contrato aprovado e assinado, recusa criar carga") e a jornada (OS 409 antes do go-live). |
| Planejamento / aceite | Parcial | Parcial | Não registrado | O que persiste é a OS liberada pelo Planejamento (`planning:manage` ou `product:manage`), com produto e campos livres; **não há colunas de capacidade, risco ou decisão**. A tela de aceite de viagens (`TripViabilityPage.jsx`) só lê a régua e calcula. O 403 de quem não é Planejamento não tem teste. Testes: `todogreen-transactions.worker.test.js` ("não pula etapas e a entrega com POD conclui e gera elegibilidade"), `tripViabilityDomain.test.js` ("abaixo do piso: recusar, com contraproposta em reais", "custo essencial faltando não vira 'aceitar' com margem inflada"), `TripViabilityPage.test.jsx` ("a régua em vigor manda na decisão, não uma cópia da tela"). |
| Operação / viagem | Sim | Sim | Não registrado | `todogreen_client_operations` (0033), pela coleção `operations` (`vertical-records/colecoes/operacao.js`), é a fonte canônica compartilhada com o portal (a antiga `todogreen_operations` saiu na 0112). Testes: `todogreen-vertical-records.worker.test.js` ("a operação criada por dentro aparece no Portal do Cliente", "persiste coordenadas de coleta/entrega e as preserva em PATCH parcial"), `todogreen-portal-operacoes.worker.test.js` ("traz prazo, ocorrências e resumo da seleção", "a lista nunca despeja a posição viva (last_position) — nem de operação entregue"). |
| Ocorrências / eventos | Sim | Sim | Não registrado | Ledger `todogreen_client_operation_events` (0045; idempotência 0094; medição 0105) via `aplicarEventoOperacional` (`vertical-records/ledger-operacional.js`). Testes: `todogreen-vertical-records.worker.test.js` ("registra ocorrências sem perder o histórico da execução", "km e energia viram fato do evento e refletem na operação vazia, sem sobrescrever depois (N.2)"), `todogreen-driver-portal.worker.test.js` ("ocorrência incrementa o contador da operação", "evento antigo sincronizado atrasado não sobrescreve a posição mais nova", "reenvio com a mesma chave devolve o evento já gravado (200) e não duplica"), `todogreen-tms-webhook.worker.test.js` ("N.4a: a ocorrência aplicada à operação ganha chave de idempotência e não duplica evento no ledger") e o passo 16 da jornada. |
| POD / comprovante | Sim | Sim | Não registrado | Entrega sem prova é recusada pelo ledger, e a OS não conclui por transição (`canonical_delivery_required`). O app do motorista captura foto do canhoto e assinatura, guardadas no cofre (0096); o POD ainda aceita só o nome do recebedor, sem arquivo. Testes: `todogreen-transactions.worker.test.js` ("não pula etapas e a entrega com POD conclui e gera elegibilidade", "a entrega com POD conclui a OS e não fica presa na ponte manual (#120)"), `todogreen-driver-portal.worker.test.js` ("registra entrega com recebedor, GPS e comprovante — e o POD da OS nasce", "guarda foto e assinatura no cofre e aponta o comprovante para o download", "recusa (400) uma entrega com 'imagem' que não é imagem"), `todogreen-tms-public-api.worker.test.js` ("cria shipment idempotente, registra tracking e conclui com POD + faturamento") e o passo 17 da jornada. |
| Portal do motorista | Sim | Sim | Parcial (H4) | Recorte por `driver_id` em toda consulta (`todogreen-driver-portal.js`; vínculos operação→motorista e motorista→e-mail na 0070); o motorista é barrado nas rotas internas no ponto único de controle. Testes: `todogreen-driver-portal.worker.test.js` ("cada motorista vê o próprio recorte", "evento na viagem de outro motorista responde 404", "o papel motorista não alcança o resto da vertical", "motorista leva 403 nas rotas internas (inclui /tracker*, o bug corrigido)", "sem cadastro ligado ao e-mail, avisa em vez de mostrar dado alheio"), e2e crítico `todogreen-portais-auth.spec.js` ("motorista cadastrado abre o portal operacional, não a central de frota"). H4 cobre só a resolução do papel num deploy paralelo. |
| Frota | Sim | Sim | Não registrado | Veículos e manutenção (0031; a tabela de eventos da 0031 não é usada), motoristas (0062), classes compatíveis, economia por placa e importação em massa (que não grava o perfil físico). Telemetria depende do rastreador. Testes: `todogreen-fleet.worker.test.js` ("operações cadastra um veículo (fleet:manage já concede)", "auditor (só leitura) não cadastra: 403", "ordem de manutenção: cria, fecha e não fecha em cima de revision velha", "soma a manutenção por veículo e o km das operações por placa", "cria os válidos, ignora inválido e placa repetida no mesmo lote"), `vehicleClassDomain.test.js` ("recusa bitrem/rodotrem no cadastro com mensagem explícita"). |
| TMS Track3r por arquivo | Sim | Sim | Não registrado | Importação, deduplicação e casamento por CNPJ (`todogreen-tms.js`, 0063); o nome da unidade substitui o CNPJ da To Do Green em origem e destino. O gate de go-live pede "arquivo real importado" (`docs/TODOGREEN_INTEGRATION_GO_LIVE.md`), sem registro até agora. Testes: `todogreen-tms.worker.test.js` ("importa, casa o embarcador por CNPJ e reconhece a classe do veículo", "reimportar o mesmo relatório não duplica", "a reimportação NÃO desfaz um vínculo casado à mão", "informa o motivo de cada linha ignorada"), `track3rDomain.test.js` ("descarta CNPJ inválido em vez de guardar chave de casamento falsa", "troca o CNPJ da To Do Green (com ou sem máscara) pelo nome"). |
| TMS Track3r API/webhook | Sim | Sim | Parcial (H3) | Receptor sem sessão com inbox (0134), recusas gravadas (0142), projetores (0135–0138), ocorrências (0080), token por endpoint e reprocessamento no cron horário — este sem teste. Falta evento real do fornecedor. Testes: `todogreen-tms-webhook.worker.test.js` ("Token errado é 401 e não grava nada", "aceita sem sessão nenhuma, com o corpo que o fornecedor espera", "reenvio do mesmo evento não duplica — vira atualização"), `todogreen-tms-webhooks-all.worker.test.js` ("recebe encomenda e preserva o payload bruto na inbox", "reenvio idêntico é idempotente e só aumenta receive_count", "registra a tentativa RECUSADA para diagnóstico, sem guardar o token", "exige Token exclusivo por endpoint quando o modo individual começa"), `todogreen-track3r-webhook-tokens.worker.test.js` ("gera 13 chaves estáveis e distintas sem colocar valores nas URLs", "falha fechada quando dois tipos têm o mesmo valor de token"). |
| Rastreamento / telemetria | Parcial | Sim | Não registrado | Posições e ponte para a operação (`todogreen-tracker.js`, 0038 e `0107_todogreen_fleet_vehicle_telemetry.sql`), retenção no cron (`0107_todogreen_tracker_position_retention.sql`). Só vira "conectado" depois de sincronização real. O cron chama a sincronização uma vez por disparo (eram duas até 24/09); o job não tem trava própria, então uma entrega duplicada do cron (o Cloudflare garante "pelo menos uma vez") sincroniza em dobro com o fornecedor. Testes: `todogreen-tracker.worker.test.js` ("carimba a última posição do tracker na operação em curso da mesma placa", "não tenta sincronizar sem segredo no cofre", "não aceita webhook sem segredo de assinatura configurado"), `todogreen-tracker-readiness.worker.test.js` ("informa saúde vazia sem inventar conexão ou dados"), `todogreen-tracker-retention.worker.test.js` ("apaga posições além da janela e preserva as de dentro"), `cron.worker.test.js` ("o disparo horário roda os jobs horários e o rastreador uma vez só"), `todogreen-driver-telemetria.worker.test.js` ("veículo sem leitura é honesto: temLeitura=false, sem inventar 0%"). |
| CIOT interno | Sim | Sim | Não registrado | Payload, estados e vínculo com a OS (0057). O frete é bloqueado abaixo do piso mínimo **informado**: sem piso no corpo não há bloqueio, e não há cálculo pela tabela de piso da ANTT. Teste: `todogreen-transactions.worker.test.js` ("prepara CIOT e bloqueia frete abaixo do piso mínimo"). |
| CIOT oficial ANTT | Preparado | Parcial | Não registrado | Conector host-side (`connectors/antt-ciot/`, `docs/todogreen-ciot-direct-connector.md`); o envio exige integração, URL e certificado, com destino restrito a `ciot:manage` e à lista `TODOGREEN_ANTT_CIOT_ALLOWED_HOSTS`. Credencial A1 cifrada no D1 (0058); sem `TODOGREEN_CIOT_VAULT_KEY`, o cofre usa o `SESSION_SECRET`. A lista de hosts, a resposta de ensaio e o upload do certificado não têm teste, e o conector fica fora do gate. Testes: `todogreen-transactions.worker.test.js` ("prepara CIOT e bloqueia frete abaixo do piso mínimo"), `tmsIntegrationsDomain.test.js` ("CIOT pela metade diz exatamente o que falta"). |

## Fiscal, faturamento e financeiro

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Fila de faturamento | Sim | Sim | Não registrado | A entrega com POD, pelo ledger, grava o POD, conclui a OS e cria o item elegível no mesmo lote (tabelas da 0056; POD obrigatório pelo gatilho da 0062); conferência e fechamento exigem `finance:manage`. A ponte "Entregas a faturar" gera a OS faturável de entrega já feita, só com contrato ativo, aprovado e assinado e implantação ativa, e é idempotente. Testes: `todogreen-transactions.worker.test.js` ("não pula etapas e a entrega com POD conclui e gera elegibilidade", "confere, fecha, prepara documento e cria contas a receber", "com contrato ativo, marca podeGerar e cria OS concluída + item elegível", "sem contrato ativo, não marca podeGerar e recusa a geração"), `transactional-spine-domain.test.js` ("entrega com POD e sem OS pede a geração da ordem, não fatura sozinha") e o passo 18 da jornada. |
| Faturamento | Parcial | Parcial | Não registrado | Fechamento, fatura e título (0056); a fatura nasce `issued` e o gatilho da 0062 a rebaixa para `prepared`; o rascunho fiscal sai por `POST /fiscal/documentos/da-fatura`. **Defeito ([D1](#defeitos)):** o segundo fechamento do mesmo tipo de documento no mesmo espaço falha com 500 — a numeração da fatura sempre devolve 1. Os testes fecham um faturamento só por espaço; dois fechamentos, a recusa em competência fechada e a regra de um cliente por fechamento não têm teste. `documentType: "nfe"` é aceito, mas não vira documento fiscal. Testes: `todogreen-transactions.worker.test.js` ("confere, fecha, prepara documento e cria contas a receber", "lista a fatura pendente e prepara o CT-e pré-preenchido, sem duplicar") e os passos 18 e 19 da jornada. |
| Motor fiscal | Sim | Sim | Não registrado | Todo o handler exige `fiscal:manage`; os impostos são recalculados no servidor ao validar; XML, número (série atômica, 0069) e chave são gerados na assinatura, só para CT-e/MDF-e (`todogreen-fiscal.js` com `fiscalDomain.js`; 0064); trilha de eventos; o cancelamento estorna o recebível sem baixa. O DACTE é PDF gerado no navegador (`pages/FiscalPage.jsx`, CODE-128C em `code128Domain.js`), sem teste de tela, e recebe a chave sem as letras do CNPJ alfanumérico ([O5](#achados-de-leitura-ainda-não-reproduzidos)). **Fronteira:** o XML da assinatura põe o motorista como remetente e deixa o destinatário vazio, embora o documento guarde tomador, remetente e destinatário. Testes: `todogreen-fiscal.worker.test.js` ("valida um CT-e completo e recusa pulo de estado", "ao assinar, o XML leva o valor real e ganha número sequencial", "dois documentos assinados nunca dividem o mesmo número (reserva atômica)", "CT-e interestadual S→SE devolve ICMS e CFOP corretos", "um espaço não vê o documento do outro", "quem só lê consulta mas não emite", "cancelar a nota cancela o título e o lançamento do razão", "recusa cancelar quando o título já teve baixa (não apaga caixa recebido)"), `fiscalDomain.test.js` ("lucro presumido: ICMS + PIS/COFINS, sem Simples", "o XML guarda o CNPJ com as letras, sem virar outro número"), `code128Domain.test.js` ("gera um código que o leitor decodifica de volta na mesma chave"). |
| CT-e / MDF-e autorizado | Preparado | Parcial | Não registrado | Só transmite com certificado, senha, URL do conector e host em `SEFAZ_CONNECTOR_ALLOWED_HOSTS`; `interpretarRetornoSefaz` exige cStat 100/104 **com protocolo**. O repositório não traz conector SEFAZ, e a chamada ao conector não tem teste de worker. O ERP aceita **registro manual** de documento autorizado com protocolo digitado e chave gerada na assinatura ou de 44 dígitos, sem conferência de dígito verificador nem consulta oficial; CT-e vindo do TRACK3R entra `validado`, com o protocolo do TMS. Emitente com CNPJ alfanumérico não tem chave gerada (a oficial vem do conector). Testes: `fiscalDomain.test.js` ("autorizado só com cStat 100 E protocolo oficial", "cStat de autorização SEM protocolo não é autorizado (não fabrica)", "resposta de ensaio nunca vira autorizado", "certificado sem conector NÃO habilita transmissão automática", "não monta chave de acesso errada para emitente com CNPJ alfanumérico"), `todogreen-fiscal.worker.test.js` ("sem certificado, 'transmitido' é recusado; documento emitido fora entra com protocolo e chave"). A ponta real com a SEFAZ é o único `it.todo` do repositório: `todogreen-erp-journey.worker.test.js` ("CT-e só pode aparecer como autorizado depois do retorno oficial da SEFAZ"). |
| NFS-e autorizada | Não | n/a | Não registrado | Só cálculo e validação (no Motor fiscal: ISS e tomador); não há XML, número nem conector de NFS-e — a transmissão só conhece os modelos 57/58, e com conector a NFS-e esbarra em "Assine o documento". O único caminho para "autorizado" é o registro manual. O Padrão Nacional aparece só como pendência (`docs/CATALOGO_RECURSOS_GRATUITOS.md`). |
| Contas a receber | Sim | Sim | Não registrado | Títulos do faturamento, do lançamento manual e das faturas TRACK3R, com espelho no razão (gatilho da 0069). Na tela, "Contas a receber" é o razão; os títulos ficam na página transacional. Cobrança bancária automatizada é externa (não há boleto nem Pix de cobrança). A leitura dos títulos não confere permissão ([L8](#lacunas-de-controle)). Testes: `todogreen-transactions.worker.test.js` ("o título a receber aparece no razão como receita (ponte 0069)", "recusa baixa do recebível-ponte pelo razão (evita dupla baixa)", "lança um recebível avulso, com saldo em aberto igual ao valor"), `todogreen-tms-webhooks-all.worker.test.js` ("projeta fatura de cliente no contas a receber e no razão") e o passo 20 da jornada. |
| Recebimento / baixa | Sim | Sim | Não registrado | Baixa de título parcial ou integral com trava otimista no saldo e recusa em mês fechado, espelhada no razão (0069); baixa e estorno do razão em `vertical-records/pagamentos.js` — a baixa do razão ignora a trava de competência de propósito; a do título, não. Não existe estorno de baixa de título (a mensagem do cancelamento fiscal pede um que não tem caminho). A trava otimista e a recusa em mês fechado da baixa de título não têm teste. Testes: `todogreen-transactions.worker.test.js` ("aceita baixa parcial e depois integral", "o título manual pode ser baixado como qualquer outro"), `todogreen-vertical-records.worker.test.js` ("registra baixas parciais com histórico e impede pagamento acima do saldo", "estorna uma baixa lançando compensatório e reabrindo o saldo, sem apagar o histórico") e o passo 20 da jornada. |
| Tesouraria / conciliação | Sim | Sim | Não registrado | Extrato com dedup (o OFX é lido na tela), conciliação 1:1 por sugestão, desfazer, fechamento e reabertura de período (`todogreen-treasury.js`, 0068). Há **duas tabelas de contas bancárias**: a do Cadastro (`todogreen_bank_accounts`, 0062) não aparece na Tesouraria (`todogreen_treasury_accounts`, 0068). A recusa de conciliar ou desfazer em mês fechado não tem teste, e a leitura não confere permissão ([L8](#lacunas-de-controle)). Conexão bancária direta seria externa. Testes: `todogreen-treasury.worker.test.js` ("importa e ignora a linha repetida na reimportação", "sugere o lançamento certo e conciliar move o saldo", "entrada no banco não concilia com custo", "não concilia duas vezes a mesma linha", "desfazer libera a linha e o lançamento", "quem só consulta lê mas não concilia", "reabrir exige motivo e deixa rastro"), `treasuryDomain.test.js` ("empate não é confiável — escolher o primeiro seria chutar"). |
| Custos / rateios | Sim | Sim | Não registrado | Rateio multidimensional (0056) em `todogreen_cost_entries`. **Fronteira:** esses custos não têm ponte para o razão nem outro leitor — ficam fora do resultado, da conciliação e do fechamento, e a criação não consulta a trava de competência; a página "Custos" do menu é o razão. Testes: `todogreen-transactions.worker.test.js` ("recusa custo sem rateio integral e grava custo multidimensional"), `transactional-spine-domain.test.js` ("exige rateio integral e pelo menos uma dimensão"). |
| Contas a pagar | Sim | Sim | Não registrado | Compras, folha, férias, rescisão, NF do PJ aprovada e faturas TRACK3R de motorista e rede terceira geram obrigações em `todogreen_financial_entries`. Só a compra ganha também o título `payable-` (gatilho da 0062, sem teste), e essa conta pode ser baixada duas vezes — no razão e no título ([O4](#achados-de-leitura-ainda-não-reproduzidos)). O título a pagar manual fica fora do razão. Pagamento bancário é externo, exceto o repasse do PJ via SysPag, dormente. Testes: `todogreen-purchasing.worker.test.js` ("entra no estoque e gera o título a pagar com vencimento pelo prazo"), `todogreen-payroll.worker.test.js` ("fechar gera as quatro contas a pagar no razão, com vencimento no mês seguinte", "fora do Simples, fechar a folha lança o INSS patronal como conta a pagar"), `todogreen-employee-portal.worker.test.js` ("gestor lista, ajusta o esperado e aprova → nasce a conta a pagar"), `todogreen-tms-webhooks-all.worker.test.js` ("projeta faturas de motorista e rede terceira como custos sem duplicar reenvio"), `todogreen-transactions.worker.test.js` ("lança um pagável avulso com prefixo PAG-"). |
| Fechamento de competência | Parcial | Parcial | Não registrado | A trava (0068) vale na coleção `financial` ao criar, editar e arquivar — o único caminho com teste —, nas transações (fechamento, baixa de título e título manual) e na tesouraria (conciliar e desconciliar). **Lacuna:** folha, férias, rescisão, recebimento de compras, NF do PJ, projeções TRACK3R e cancelamento fiscal gravam ou alteram o razão sem consultar a trava ([L3](#lacunas-de-controle)). Testes: `todogreen-treasury.worker.test.js` ("mês fechado recusa lançamento novo", "mês fechado recusa alteração e arquivamento do que já existe", "não deixa empurrar lançamento para dentro do mês fechado", "fechar duas vezes o mesmo mês responde 409", "o fechamento de um espaço não trava o outro"), `treasuryDomain.test.js` ("bloqueia lançamento com competência em mês fechado"). |
| Plano de contas e centros de custo | Sim | Sim | Não registrado | Tabelas da 0053; coleções `accounts` e `costCenters` (`vertical-records/colecoes/cadastros.js`), escrita com `finance:manage`, na tela Cadastros › Financeiro; o razão guarda conta e centro de custo (0068). Testes: `todogreen-erp-core.worker.test.js` ("cria conta com natureza válida e recusa a inventada", "guarda a marcação de sintética", "cria centro de custo e exige nome"). |
| Cobrança com multa e juros | Sim | Sim | Não registrado | `GET /treasury/cobranca` (`todogreen-treasury.js`, colunas de multa e juros da 0068) compõe o valor a cobrar; não emite boleto nem Pix. Testes: `todogreen-treasury.worker.test.js` ("mostra a composição do que cobrar hoje", "não cobra encargo antes do vencimento", "percentual negativo não gera crédito"). |
| Resultado por centro de custo e conta | Sim | Sim | Não registrado | `GET /treasury/resultado` (`resultadoPorEixo`, `treasuryDomain.js`), que também é o retrato do fechamento; não inclui os custos rateados. Testes: `todogreen-treasury.worker.test.js` ("agrupa por centro de custo e por conta"), `treasuryDomain.test.js` ("margem é null sem receita, não 0%", "ignora lançamento cancelado"). |
| Previsão de caixa, aging e recorrência | Parcial | Parcial | Não registrado | `contasPonteDomain.js` calculado no navegador (Tesouraria e Financeiro), sem endpoint no servidor. Só teste de domínio: `contasPonteDomain.test.js` ("classifica o atraso por faixa e ignora cancelados e quitados", "projeta a semana: atrasado cai na primeira, receita menos custo", "recorrência mensal respeita mês curto e devolve vazio sem data"). |
| Comissões | Parcial | Parcial | Não registrado | Só lançamento manual na coleção `financial` com tipo comissão (escrita com `finance:manage`; `commission:manage` só lê), na página Comissões; entra no resultado. Não há cálculo de comissão a partir de venda, contrato ou recebimento. Teste: `todogreen-vertical-records.worker.test.js` ("receita, custo e comissão convivem na mesma coleção"). |
| Dados da empresa e documentos regulatórios | Sim | Parcial | Não registrado | `todogreen_company_profiles` e `todogreen_company_documents` (0062) em `todogreen-master-data.js`, com `fiscal:manage` ou `finance:manage`; lidos pelo CIOT. Duplicam o perfil fiscal (`todogreen_tax_profiles`, 0064) e gravam o CNPJ só com dígitos ([O5](#achados-de-leitura-ainda-não-reproduzidos)). Sem teste de worker; só `masterRegistryConfig.test.js` ("companyProfiles: reconstrói addressText e cep a partir de address"). |
| Contas bancárias do cadastro | Sim | Parcial | Não registrado | `todogreen_bank_accounts` (0062) em `todogreen-master-data.js`, com `finance:manage` ou `hr:manage`; guarda o PIX do PJ. Não alimenta a Tesouraria (ver Tesouraria / conciliação). Testes: `todogreen-employee-portal.worker.test.js` ("grava (validada) e a sessão passa a mostrá-la", "CLT não grava PIX aqui (é do RH) — 403"). |

## Compras, suprimentos e estoque

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Requisição | Sim | Sim | Não registrado | Itens, requisitante, centro de custo e máquina de estados (0055; devolução e gestão em `0087_todogreen_purchase_request_triagem.sql`); qualquer papel cria e reenvia, a triagem exige `purchase:manage`. O requisitante vem do corpo, o que abre a [L10](#lacunas-de-controle). Testes: `todogreen-purchasing.worker.test.js` ("cria pendente para Suprimentos e aceita item por descrição livre", "segue o caminho declarado e recusa pulo de etapa", "um espaço não vê a requisição do outro", "qualquer área requisita, mas só Suprimentos tria e compra"), `purchaseDomain.test.js` ("Suprimentos pode devolver, recusar ou direcionar à gestão o que está pendente"). |
| Alçada de compras | Parcial | Sim | Não registrado | Faixas **configuráveis por espaço** (0111, `GET/PUT /api/todogreen/purchasing-params`, `PurchaseApprovalPanel.jsx`), com auditoria e revisão otimista só quando o corpo manda `revision` — sem ela, o PUT sobrescreve —, e sem histórico das versões anteriores. O portão (`todogreen-purchasing-enterprise.js`, ligado em `worker-entry.js`) é o único que escreve a trilha de aprovação, e a aprovação só leva a decisão ([L4](#lacunas-de-controle), corrigida). **Lacuna:** quem cria a requisição em nome de outra pessoa faz a primeira aprovação — na régua de fábrica, até R$ 5 mil, a única ([L10](#lacunas-de-controle)). Nenhum teste de worker completa uma alçada de várias etapas. **Decisão pendente:** aprovações parciais sobrevivem a uma edição de valor entre etapas (a régua é recalculada e mantém as etapas compatíveis); se a política for "a aprovação vale para o valor aprovado", a trilha precisa guardar o total e recomeçar quando ele muda. Testes: `todogreen-purchasing.worker.test.js` ("status com espaço recebe a mesma alçada do status exato", "trilha de aprovação enviada na criação não é gravada", "trilha enviada numa edição não vale, e a verdadeira sobrevive à edição", "aprovar não troca os itens do pedido no mesmo clique", "colaborador em espaço alheio não aprova a própria requisição; a dona aprova"), `todogreen-purchasing-params.worker.test.js` ("PUT grava uma régua válida, versiona e persiste", "PUT com revisão velha responde 409", "auditor lê mas não edita"), `purchaseApprovalEnterprise.test.js` ("sobe a aprovação conforme o valor", "recalcula a régua sem apagar aprovações compatíveis"), `PurchaseApprovalPanel.test.jsx` ("papel sem edição só lê"). |
| Cotação / fornecedor | Parcial | Parcial | Não registrado | Fornecedores na coleção `parties` (0053, `vertical-records/colecoes/cadastros.js`), e o pedido herda o prazo deles; `rfq_id` só é gravado se vier no corpo, e nenhuma tela o envia. A **comparação de ofertas não está no ERP** (só no app geral, `features/procurement/`). Testes: `todogreen-erp-core.worker.test.js` ("recusa CNPJ inválido preenchido, mas aceita documento ausente", "exige ao menos um papel conhecido", "operações vê o fornecedor, que não pertence a carteira nenhuma"), `todogreen-purchasing.worker.test.js` ("recusa fornecedor de outro espaço"). |
| Pedido de compra | Parcial | Sim | Não registrado | Pode nascer de requisição aprovada ou avulso (a requisição informada precisa estar aprovada), com o total aprovado guardado como retrato. **Lacuna:** editar depois de aprovado só marca `aprovacaoValida: false` — o pedido continua `aprovado` e pode ser enviado e recebido ([L9](#lacunas-de-controle)). Testes: `todogreen-purchasing.worker.test.js` ("recusa gerar pedido de requisição não aprovada", "grava o total aprovado como retrato", "editar depois de aprovado derruba a validade da aprovação", "pedido enviado não tem os itens alterados", "a segunda gravação com a mesma revisão recebe 409"), `purchaseDomain.test.js` ("não pula etapa: rascunho não vai direto a enviado"). |
| Recebimento | Sim | Sim | Não registrado | Movimento de estoque e título; imutável (405) e limitado ao que falta receber — mas **não é idempotente**: reenviar o POST de um recebimento parcial grava outro, e a devolução sai do estoque sem a conferência de saldo do serviço de estoque ([O8](#achados-de-leitura-ainda-não-reproduzidos)). Lança no razão sem consultar a trava (L3); chave de NF-e com letras é recusada ([O5](#achados-de-leitura-ainda-não-reproduzidos)). Testes: `todogreen-purchasing.worker.test.js` ("entra no estoque e gera o título a pagar com vencimento pelo prazo", "recusa receber mais do que falta", "vários recebimentos somam até completar", "devolução sai do estoque e não pode passar do recebido", "recusa chave de nota com tamanho errado", "recebimento não é editado nem apagado"), `purchaseDomain.test.js` ("monta o título com vencimento contado do recebimento"). |
| Estoque | Sim | Sim | Não registrado | Movimentos append-only e contagens (0054): saldo conferido e gravado numa instrução só (409 acima do saldo), transferência entre depósitos e inventário; PATCH e DELETE respondem 405. O 409 vale para `/api/todogreen/stock`; a devolução de compra passa por fora. A leitura não confere permissão ([L8](#lacunas-de-controle)). WMS externo seria integração. Testes: `todogreen-stock.worker.test.js` ("responde 409 com o saldo disponível e não grava nada", "o saldo é por depósito: ter na matriz não libera saída na filial", "baixa na origem e entra no destino, ligadas pelo mesmo grupo", "divergência gera movimento de ajuste auditável", "quem só consulta lê o saldo mas não movimenta"), `stockDomain.test.js` ("a saída consome pela média vigente e NÃO altera a média"). |
| Cadastros de suprimentos | Sim | Sim | Não registrado | Coleções `items` e `warehouses` (escrita com `stock:manage`) e `parties` (escrita com `purchase:manage`) em `vertical-records/colecoes/cadastros.js`, tabelas da 0053; a parte aceita CNPJ alfanumérico. Testes: `todogreen-erp-core.worker.test.js` ("cria, normaliza o SKU e nasce com revisão 1", "recusa unidade que não existe em vez de assumir UN", "um espaço não vê o material do outro", "quem só consulta não altera cadastro", "a segunda gravação com a mesma revisão recebe 409"), `erpCoreDomain.test.js` ("aceita o CNPJ alfanumérico com o dígito verificador oficial (ASCII − 48)"). |
| Previsão de compra (reposição) | Parcial | Parcial | Não registrado | `previsaoDeCompra` (`purchaseDomain.js`) na tela de Compras; não gera requisição nem grava nada. Só teste de domínio: `purchaseDomain.test.js` ("sugere reposição só para itens abaixo do mínimo, com custo estimado"). |

## RH, Departamento Pessoal e pessoas

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Cadastro de colaboradores | Sim | Parcial | Não registrado | Duas portas, ambas com `hr:manage`: `/payroll/colaboradores` (CPF validado e mascarado na lista) e o cadastro mestre `/master-data/employees` com documentos (0062, colunas na 0066) — o da tela Cadastro · Colaboradores, que só exige nome, não valida o CPF e lista o documento inteiro. O cadastro mestre e os documentos não têm teste. Testes: `todogreen-payroll.worker.test.js` ("vendedor sem hr:manage recebe 403 em tudo", "sem sessão, 401", "cria colaborador válido e recusa CPF inválido", "na lista o CPF vem mascarado; no detalhe, inteiro", "um espaço não vê o colaborador do outro"), `payrollDomain.test.js` ("valida CPF real e recusa inválido"). |
| RH operacional / alocação | Parcial | Sim | Não registrado | Disponibilidade do motorista (0062), ocupada pelo despacho e liberada pela última entrega; turno registrado pelo próprio motorista (0110); ponto do colaborador digitado pelo RH (0066). **Não há escala**, nem ligação do turno com o ponto. Testes: `todogreen-payroll.worker.test.js` ("registra ponto e férias de um colaborador e lista de volta", "extras geram DSR (provento) e faltas descontam do líquido e da base"), `todogreen-driver-portal.worker.test.js` ("inicia o turno, recusa iniciar de novo e encerra com horas derivadas", "a jornada é do próprio motorista: o turno da Maria não aparece para o João"), `todogreen-dispatch.worker.test.js` ("eventos canônicos avançam a rota e liberam motorista + veículo no final") e o passo 14 da jornada. |
| Folha | Parcial | Parcial | Não registrado | Cálculo, itens, fechamento e reabertura (0066); a reabertura é exercitada nos dois primeiros casos citados abaixo. **Lacunas:** a única tabela de INSS/IRRF/FGTS é `TABELAS_2025` (`payrollDomain.js`, versão "2025.1"), embutida no código; a API aceita folha de adiantamento, férias e rescisão, mas só a mensal e o 13º têm cálculo próprio (os outros viram holerite de salário cheio), e a tela só abre a mensal; lança no razão sem a trava (L3). O rastro da reabertura não é conferido, e os tipos além de mensal e 13º não têm teste. Testes: `todogreen-payroll.worker.test.js` ("fecha calculando o holerite e não fecha de novo", "fechar gera as quatro contas a pagar no razão, com vencimento no mês seguinte", "fecha por tipo decimo_terceiro, proporcional aos meses, e exclui quem entrou depois", "fecha a folha pulando e sinalizando quem ainda não tem salário base"), `payrollDomain.test.js` ("um centavo acima da fronteira já toca a segunda faixa", "uma versão nova produz outro número e diz de qual versão veio"). |
| Férias | Parcial | Sim | Não registrado | Pagamento real (proporcional + 1/3) com holerite e lançamentos. **Lacunas:** abono pecuniário e adiantamento do 13º são gravados e ignorados no cálculo; o status é o da criação, sem rota para mudá-lo; nada confere o período aquisitivo; lança no razão sem a trava (L3). Testes: `todogreen-payroll.worker.test.js` ("paga as férias (proporcional + 1/3), grava o holerite e lança no razão", "recusa pagar sem início de gozo e com revision defasada"), `payrollDomain.test.js` ("férias somam o terço constitucional", "férias proporcionais a 15 dias"). |
| Rescisão | Parcial | Sim | Não registrado | Cálculo, desligamento e obrigações **somente para dispensa sem justa causa** (o domínio declara e a tela avisa); lança no razão sem a trava (L3). A descrição das férias proporcionais mostra os avos do 13º, embora o valor use os do período aquisitivo. eSocial, FGTS Digital e banco são externos. Testes: `todogreen-payroll.worker.test.js` ("calcula as verbas, desliga o colaborador e lança no razão", "recusa sem data de desligamento e com revision defasada"), `payrollDomain.test.js` ("aviso prévio: 30 dias + 3 por ano completo, indenizado por padrão", "férias proporcionais pelos avos do PERÍODO AQUISITIVO, não do ano", "multa de 40% do FGTS só com saldo informado; senão null"). |
| eSocial / obrigações | Não | n/a | Não registrado | Só a flag `payrollTransmissionEnabled`; não há código de transmissão nem de geração de arquivo — embora a tela diga que eSocial e FGTS "ficam como geração de arquivo" e mostre "Pronto" com o certificado no cofre. |
| Pagamento bancário da folha | Não | n/a | Não registrado | Não há CNAB; o adaptador SysPag atende GreenPay e a NF do PJ, não a folha. |

## Jurídico, Qualidade, Marketing e processos internos

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Workflow Jurídico | Parcial | Parcial | Não registrado | Dois fluxos. **Canônico:** a Central Jurídica (`src/features/legal/LegalHub.jsx`) usa `/records/legal` (0090) e `/records/legal/:id/events` (0104), com máquina de estados (`resolverAcaoJuridica`) e decisão por `compliance:manage` — contornável pela L1; o endpoint de eventos não tem teste de worker. **Legado:** o fluxo de duas etapas (Jurídico + liderança) de `todogreen-enterprise-workflows.js` (0076) tem painel que nenhum menu monta; o gate do contrato o lê só pela etapa `juridico` (O2), e o PATCH desse fluxo aceita `status` arbitrário (L6), o que não abre o gate. Testes: `todogreen-enterprise-workflows.worker.test.js` ("exige a nota, registra a ressalva e faz o fluxo avançar sem reprovar", "reprovar continua encerrando o processo"), `todogreen-vertical-records.worker.test.js` ("cria, lista, atualiza situação e recusa documento sem título", "quem só lê não cria documento jurídico") — o primeiro trava como esperado o PATCH direto de `situacao`, isto é, a L1 —, `legalDomain.test.js` ("resolve marcar_assinado como transição auditada de aprovado para assinado") e o passo 8 da jornada. |
| Negociação contratual | Parcial | Parcial | Não registrado | Pontos de negociação e "com quem está a bola" (`contratoNegociacaoDomain.js`) gravados no fluxo empresarial por um PATCH de mescla rasa; a única tela é o painel do domínio legal, que não é montado. Nenhum código de servidor lê nem exige que estejam resolvidos; a Central Jurídica tem a situação "Em negociação", que vira `rascunho` no registro canônico. Só teste de domínio: `contratoNegociacaoDomain.test.js` ("alternar marca como acordado e desmarcar reabre — nada é apagado", "o resumo conta abertos e acordados e só dá resolvido com todos acordados", "a bola só troca entre os dois lados do vaivém"). |
| Qualidade / CAPA / não conformidade | Parcial | Parcial | Não registrado | A tela Qualidade grava NC real na coleção `quality` (0089, `vertical-records/colecoes/qualidade.js`, `operations:manage`), com situação livre e sem etapa. O plano CAPA sequencial fica no fluxo empresarial (domínio `quality`: CAPA, auditoria e NC) — contornável (L6) e com painel que nenhum menu monta. Testes só do registro de NC: `todogreen-vertical-records.worker.test.js` ("cria, lista, atualiza situação e recusa NC sem título", "valores fora da lista caem no padrão em vez de gravar lixo", "quem só lê não cria não conformidade"), `QualityPage.test.jsx` ("registra uma nova não conformidade com título obrigatório", "muda a situação de uma NC pela própria lista"). |
| Marketing / campanhas | Parcial | Não | Não registrado | Aba Campanhas na página de marketing, com alçada por orçamento (acima de 5 mil, liderança; acima de 25 mil, financeiro). **Contornável além da L6:** a própria tela deixa desmarcar "Exigir fluxo de aprovação" e concluir sem aprovação; até 5 mil há uma etapa só, que quem abriu aprova sozinho; e a alçada é recalculada pelo orçamento atual, então baixar o orçamento antes da decisão tira etapas. Sem teste. |
| Processos internos gerais | Parcial | Parcial | Não registrado | O domínio `general` não tem etapas (`approvalPlan` vazio) nem tela. A recorrência roda no cron horário e tem teste; a criação e a edição pela API, não. Teste: `cron.worker.test.js` ("o disparo horário roda os jobs horários e o rastreador uma vez só", "o disparo manual, sem cron, continua rodando os jobs horários"). |
| Planner / Projetos / Work Center | Sim | Sim | Não registrado | Boards, membros (0077), visibilidade e recorrência (0030/0065/0067). A tarefa canônica vive em `db.tasks` (ver "To Do Green — task canônica" no `AGENTS.md`). A tela do Planner segue o formato do Microsoft Planner (quadro com baldes editáveis, tabela, linha do tempo e gráficos); arrastar um cartão grava pela mesma tarefa canônica, sem caminho paralelo. Para abrir o Planner basta `read`; toda escrita (criar, compartilhar, arquivar) segue exigindo `planner:manage`. Compartilhar só promete o que o servidor cumpre: `/planner/pessoas` marca quem alcança a vertical no espaço (`alcancaEspacoNaVertical`), e criar ou editar o plano devolve `membrosSemAcesso` — a pessoa fica na lista e passa a ver o plano quando for liberada em Acessos. A partilha do plano desce para as tarefas dele em `db.tasks` (`aplicarPartilhaDoPlanoNaTarefa`), mas só nas tarefas de quem é dono delas: para os demais, `mergeRecordsFromMember` (`worker/services/workspace.js`) tranca `sharedWith` e `visibility`. O aviso na tela e a chamada de `onSyncPlanSharing` não têm teste de tela. Testes: `PlannerPage.test.jsx` ("arrastar um cartão para outro balde grava a tarefa canônica com o novo balde", "adicionar e renomear balde fazem PATCH no plano com a revision lida", "'Quadro To Do' continua levando ao lugar único de tarefas"), `plannerBoardDomain.test.js` ("renomear preserva o id (a tarefa aponta para ele) e mover troca a ordem"), `todogreen-planner.worker.test.js` ("sem planner:manage, o auditor lê mas não cria (403)", "a colega vê o compartilhado, não vê o privado", "bloqueia escrita direta de tarefa sem opt-in legado", "só quem criou mexe na lista; tirar a pessoa fecha a porta na hora", "/pessoas marca quem alcança o Planner neste espaço", "o plano aceita a pessoa na lista, mas avisa que ela ainda não alcança; a vertical a recusa (403)"), `plannerIntegrationDomain.test.js` ("tarefa criada pelo Planner num plano com pessoas específicas nasce compartilhada com elas, para editar", "plano 'todo o espaço' liga espaco_todo; voltar a privado desliga só o que o Planner ligou"), `shellNavegacao.test.js` ("telas restritas declaram a permissão que o menu já usa"), `todogreen-work-automations.worker.test.js` ("bloqueia conclusão com dependência pendente e cria a próxima recorrência"), `WorkViews.test.jsx` ("mostra o resumo e desenha o Gantt com caminho crítico por padrão"). |
| Metas | Sim | Sim | Não registrado | 0046/0050, `todogreen-goals.js`. Testes: `todogreen-goals.worker.test.js` ("vendedor enxerga somente a meta vinculada a ele", "vendedor não cria nem altera o alvo", "meta com evidência obrigatória recusa check-in sem comprovação", "registra check-in, snapshot e histórico sem reescrever o passado", "protege alteração concorrente da meta"), `GoalsPage.test.jsx` ("permite ao administrador criar uma métrica com critérios próprios"). |
| Governança / auditoria | Sim | Parcial | Não registrado | `todogreen_audit_events` (0052) e `/api/todogreen/governance` (`audit:read`). A gravação é conferida em outras suítes; o endpoint de leitura e a `GovernancePage.jsx` não têm teste. Política de retenção/backup a definir. Testes: `todogreen-client-briefing.worker.test.js` ("a alteração fica na auditoria da vertical"), `todogreen-preflight.worker.test.js` ("WARNING exige justificativa; com justificativa grava o override auditado e a rota nasce com status WARNING"). |

## ESG, evidências e documentos

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Impacto ambiental | Sim | Sim | Não registrado | Cálculo auditável com entradas, passos e versão do fator, gravado em `environmental_calculations` (0027) por `todogreen-esg.js`; a régua ambiental é a 0103, e o fechamento mensal, a 0108. Testes: `todogreen-esg.worker.test.js` ("cada cálculo vai para o banco com entradas, passos e versão do fator", "operação inválida é reportada sem derrubar o lote", "auditor NÃO calcula — calcular grava registro auditável"), `todogreen-esg-regua.worker.test.js` ("com régua que sobe o fator da rede, o CO2 evitado cai e a versão gravada é a da régua", "operação com energia medida grava a energia real, não a derivada"), `esgEngineDomain.test.js` ("a mesma entrada dá o mesmo resultado"), `EsgCenter.test.jsx` ("manda calcular no servidor, com a origem do dado junto"). |
| Green Score | Sim | Sim | Não registrado | Score persistido (0033, isolado por espaço na 0047) e pesos versionados (0074). Indicador proprietário, não certificação. Testes: `todogreen-esg.worker.test.js` ("o segundo cálculo explica o que mudou", "régua que não soma 100 é recusada antes de gravar", "score novo nasce com a régua nova, e o antigo mantém a dele", "o benchmark posiciona sem revelar quem está na base"), `todogreen-customer-portal.worker.test.js` ("o resumo traz a comparação de emissões e a composição/variação do Green Score"), `greenScoreDomain.test.js` ("diz que não é certificação"). |
| Evidências ESG/operacionais | Sim | Sim | Não registrado | `todogreen_evidences` (0034); impressão digital dos bytes; links temporários com hash do token (0043); o portal lista só as do cliente. Testes: `todogreen-evidences.worker.test.js` ("a impressão digital sai dos bytes do arquivo, não do título nem da URL", "endereço de rede interna é recusado", "link vencido não abre", "o banco guarda o hash do token, nunca o token"), `todogreen-customer-portal.worker.test.js` ("as evidências são só as do cliente da sessão", "não emite link para evidência de outro cliente"). |
| Cofre de documentos | Sim | Sim | Não registrado | SHA-256, versões e download autenticado (0076); pastas (0084); anexos por contexto (`0088_todogreen_internal_files_context.sql`); referência externa sem bytes (`createReference`, a base da L2). Os bytes ficam em chunks no D1: o binding R2 opcional `MEDIA_BUCKET` (coluna `r2_key` em `0112_todogreen_internal_files_r2.sql`) não está declarado no `wrangler.jsonc` (`docs/SECRETS.md`). Nenhum teste confere o valor do SHA-256 nem o incremento da versão. Testes: `todogreen-file-vault.worker.test.js` ("O TESTE QUE IMPORTA: o download de arquivo em pasta invisível responde 404", "arquivar arquivo de pasta invisível responde 404, não apaga às cegas", "prende o anexo ao contexto e filtra pelo par contextType+contextId"), `todogreen-file-store-r2.worker.test.js` ("com binding: grava no R2, marca r2_key e NÃO cria chunks no D1", "sem binding: cai no D1 com chunks e r2_key nulo"). |
| Documento contratual | Parcial | Parcial | Não registrado | O anexo que o gate de assinatura lê aceita referência de 0 byte (L2), e nenhuma tela montada anexa com o contexto que o gate espera (O3). Mesmas evidências da linha Assinatura contratual: `todogreen-vertical-records.worker.test.js` ("versiona alterações e preserva a trilha do ciclo contratual"). |
| Relatório ESG do cliente e fechamento mensal | Sim | Sim | Não registrado | `todogreen-esg.js` (relatório por cliente e período, lista de clientes e fechamento mensal, 0108), no recorte de carteira; formatos em `esgReportDomain.js`; tela Relatórios e relatório do portal. Testes: `todogreen-esg-report.worker.test.js` ("devolve operações do cliente e do período pedido", "o vendedor não gera relatório fora da carteira", "registra quem gerou, para o documento ter responsável"), `todogreen-esg-regua.worker.test.js` ("fecha o mês: consolida cálculos + atividade tonne-km, e não duplica ao refechar"), `todogreen-customer-portal.worker.test.js` ("o relatório traz só o período e o cliente da sessão", "leitor não exporta relatório"). |

## Integrações

SEFAZ, ANTT e TRACK3R estão nas linhas de Operação e de Fiscal (CT-e / MDF-e
autorizado, CIOT oficial ANTT, TMS Track3r).

| Integração / capacidade | Implementado | Testado | Homologado | Situação |
| --- | --- | --- | --- | --- |
| Cloudflare Worker + D1 + Cron | Sim | Sim | Parcial (H1, H2, H7) | Padrões de cron em `worker/lib/cron.js`, iguais aos de `wrangler.jsonc`; o disparo semanal roda só o resumo. Publicação e smoke registrados; não há registro de execução de cron em produção. Testes: `cron.worker.test.js` ("os padrões do código são os de triggers.crons no wrangler.jsonc", "o disparo semanal roda só o resumo da semana", "o disparo horário roda os jobs horários e o rastreador uma vez só"), `weekly-summary.worker.test.js` ("não envia o mesmo resumo duas vezes quando o Cron dispara em dobro"), `automations-scheduled.worker.test.js` ("o disparo semanal não repete os jobs de hora em hora"). |
| Versão publicada (`GET /api/system/version`) | Sim | Sim | Registrado (H1) | SHA, build, branch, `publishedBy`, ambiente e migrações esperadas, sem segredo. Teste: `system-health.worker.test.js` ("é público, identifica o build e não expõe segredo", "só aceita GET"). |
| Saúde do sistema (Administração) | Sim | Sim | Não registrado | `/api/todogreen/system-health` (`integration:manage` ou `audit:read`), eventos (0085) e métricas (0120). O runbook descreve a conferência, sem resultado registrado. Testes: `system-health.worker.test.js` ("exige sessão", "vendedor sem papel administrativo recebe 403", "auditor (audit:read) lê o relatório", "admin recebe componentes, integrações e comparação de versões — sem segredo", "grava latência e volume e devolve último sucesso/falha por integração"). |
| Automações internas | Sim | Sim | Não registrado | Blob (0018) e Central (0049) no cron horário. Testes: `automations-scheduled.worker.test.js` ("cria tarefas e lembretes com o app fechado e não duplica o período", "ignora regras desligadas e regras que ainda não venceram"), `todogreen-work-automations.worker.test.js` ("processa prazo vencido pelo cron sem abrir o cartão"). |
| Webhooks de saída | Parcial | Parcial | Não registrado | O motor (`worker/services/webhooks.js`) só observa coleções do app geral, ao gravar o espaço; **nenhum evento das tabelas `todogreen_*`** (OS, POD, fatura, ocorrência) dispara webhook. Testes só do app geral: `webhooks-api.worker.test.js` ("gravar o espaço de trabalho dispara o aviso de verdade", "falha ao avisar não derruba a gravação dos dados"), `webhooks.worker.test.js` ("todo evento do catálogo declara coleção e campos"). |
| API pública do TMS / idempotência | Sim | Sim | Não registrado | `/api/tms/v1/*`, resolvida antes do roteador interno, com chaves `tdg_live_*` e idempotência (`0088_todogreen_tms_public_api.sql`). Testes: `todogreen-tms-public-api.worker.test.js` ("publica OpenAPI sem autenticação e exige chave nas rotas privadas", "cria shipment idempotente, registra tracking e conclui com POD + faturamento", "impede uma chave vinculada a cliente de consultar outro cliente"), `todogreen-routing-api.worker.test.js` ("exige o escopo routing:write"). |
| Pesquisa web pública | Sim | Sim | Não registrado | Cascata de provedores (`web-search.js`), com chaves do espaço cifradas (0073); o resultado passa pela heurística do Prompt Guard antes de chegar à IA. A qualidade depende da fonte. **Fronteira:** não há tempo limite por provedor — em 24/09, no ambiente de teste, um DuckDuckGo lento (15 s, depois 503) atrasou a resposta do radar na mesma medida. Testes: `web-search.test.js` ("cai para o próximo provedor só quando o primeiro não entrega", "com o provedor de cima saudável, os gratuitos nem são chamados"), `search-keys.worker.test.js` ("guarda cifrada e a coloca na cascata pelo nome da variável"), `prompt-guard.test.js` ("descarta o trecho malicioso e conta o descarte"). |
| IA BYOK | Sim | Sim | Não registrado | `ai-keys.js` (0072), cifrado com `WORKSPACE_AI_VAULT_KEY`; cada provedor depende de chave e teste positivo. Testes: `ai-keys.worker.test.js` ("guarda cifrada e nunca devolve o segredo para a tela", "sem cofre configurado, não guarda — e diz por quê", "providerChain põe o provedor do espaço antes dos gratuitos da plataforma", "a vizinha não enxerga a chave da dona"). |
| IA em cascata: rota sensível (LGPD) e IA local | Sim (IA local: Preparado) | Sim | Não registrado | `runWithFallback` (`worker/services/ai.js`) manda pedido com CPF, cartão, senha, conta ou termo clínico só a quem não treina com o conteúdo; passam por ela o Plantû, o assistente do Portal e a extensão. A IA local só entra com endereço **e** modelo do Ollama ou do vLLM. Sem teste de worker pelo Plantû. Testes: `ai-providers.test.js` ("um pedido com CPF nunca chega ao Gemini gratuito, mesmo com a chave cadastrada", "tira da cascata quem treina com o conteúdo ou depende do provedor final", "só liga Ollama/vLLM com endereço E modelo, e aparece no catálogo"). |
| AI Gateway da Cloudflare | Preparado | Sim | Não registrado | `worker/services/ai-gateway.js` na frente da cascata: liga com `AI_GATEWAY_ID` (os provedores por URL, só com `AI_GATEWAY_TOKEN`); nunca guarda conteúdo, impede cobrança sem chave e, se o gateway falhar, chama o provedor direto. O `/api/status` diz só se está ligado. O streaming do chat segue direto ao provedor. Testes: `ai-gateway.test.js` ("só liga com um ID válido e nunca expõe token no status", "Workers AI vai sem log (o conteúdo nunca fica guardado) e o sensível sem cache", "chamada por URL guarda só metadados, bloqueia cobrança sem chave e protege o sensível", "falha do próprio gateway cai para o provedor direto e pausa o gateway", "sem AI_GATEWAY_ID, nada muda: o provedor é chamado direto"). |
| Prompt Guard (texto de terceiro que chega a um agente) | Sim | Sim | Não registrado | `worker/services/prompt-guard.js`: heurística local sempre ligada (busca na web, pesquisa de empresa, radar) e o Llama Prompt Guard 2 na Groq quando há `GROQ_API_KEY` — em "Preview" na Groq, com volta para a heurística em qualquer falha. Barra injeção na pergunta do assistente do Portal do Cliente e limpa o resultado de ferramenta do Plantû. Testes: `prompt-guard.test.js` ("pega a ordem clássica: %s", "não confunde texto de negócio com ataque: %s", "sem GROQ_API_KEY decide só pela heurística, sem rede", "modelo retirado (404) pausa a Groq e volta para a heurística", "limpa dado de ferramenta sem mexer no resto"), `todogreen-customer-portal.worker.test.js` ("pergunta que tenta mandar na IA recebe a mesma recusa (Prompt Guard)"). |
| Anti-robô (Turnstile) na entrada e nas páginas públicas | Preparado | Sim | Não registrado | `worker/lib/turnstile.js` exige o token no cadastro, no login, no "esqueci a senha", no reenvio do código, no pedido de acesso à To Do Green e nos formulários públicos — só com `TURNSTILE_SITE_KEY` **e** `TURNSTILE_SECRET_KEY`; com uma só, nada muda. Siteverify fora do ar deixa passar (o limite por IP e por conta continua valendo); recusa explícita barra. Nas telas, o widget fica em `src/features/auth/Login.jsx`. Testes: `turnstile.worker.test.js` ("só liga com as duas chaves", "siteverify fora do ar não tranca o cadastro; recusa explícita tranca", "com as chaves, cadastro sem token é recusado antes de criar a conta", "login sem token é barrado antes de conferir a senha", "pedido de acesso à To Do Green também pede o token", "formulário público desenha o widget, libera a CSP e exige o token no envio"), `turnstile-auth.test.jsx` ("com a chave pública no /api/config, o login leva o token do widget", "sem chave no servidor, nada é carregado e o login segue como antes"). |
| E-mail transacional (Brevo) | Sim | Parcial | Não registrado | Em uso em convites, portal, avisos e Central (`worker/mensageria/envio.js`); o convite To Do Green vale pelo link, com ou sem Brevo. Nenhum teste do gate faz a chamada ao Brevo — o ambiente de teste não tem a chave, e o outbox usa um atalho que responde sem rede: o testado é o caminho sem Brevo e a tradução do erro. Em 24/09 o `/api/config` de produção indicava remetente vazio (`docs/SECRETS.md`). Testes: `outbox.worker.test.js` ("envia e-mail automaticamente e registra na caixa unificada"), `todogreen-send-email.worker.test.js` ("sem credencial de e-mail no cofre, avisa em vez de falhar"), `team-invites.worker.test.js` ("cria o convite e devolve o link mesmo sem e-mail configurado (o admin copia e envia)"), `email-failure-reason.test.js` ("explica o bloqueio de IP do Brevo", "explica remetente não validado"). |
| Aviso ao cliente por e-mail | Sim | Não | Não registrado | `notificarPortalDoCliente` (`todogreen-notify.js`) na entrega, na ocorrência e na resposta de solicitação; só envia com Brevo configurado e portal ligado, e engole a falha. Nenhum teste exercita o envio. |
| WhatsApp | Preparado | Parcial | Não registrado | Envio pela Cloud API ou pela Evolution e receptor de entrada, dormentes sem segredo. Nos testes, o envio pela Cloud API cai num mock; só a Evolution é testada com `fetch` simulado. **Sem `WHATSAPP_APP_SECRET` o webhook de entrada aceita POST sem conferir assinatura**, e a conferência da assinatura não tem teste. Testes: `whatsappDelivery.test.js` ("somente habilita o canal com a configuração completa", "envia somente para o telefone informado usando a instância configurada"), `inbound.worker.test.js` ("verifica e recebe mensagens do WhatsApp Cloud API"), `outbox.worker.test.js` ("envia WhatsApp pela Cloud API e registra o id do provedor"). |
| Notificações do navegador (Web Push) | Preparado | Sim | Não registrado | Assinaturas (`push-subscriptions.js`, 0012) e envio (`worker/mensageria/envio.js`), usados pelo resumo semanal e pelos avisos de pendência da vertical; dormente sem o par VAPID (em 24/09 a produção devolvia `vapidPublicKey: null`). Testes: `web-push.worker.test.js` ("envia um push para o destinatário quando uma notificação nova é sincronizada", "remove a assinatura quando o push retorna 410 (assinatura expirada)"), `weekly-summary.worker.test.js` ("envia um push com o resumo da semana anterior para quem teve atividade"). |
| Google Login | Sim | Não | Parcial (H5) | `/api/auth/google` confere emissor, audiência e e-mail verificado do ID token; nenhum teste chama a rota (a tela tem teste do botão, que não valida token). H5 é anterior ao domínio atual. |
| Gmail / Google Agenda | Parcial | Parcial | Parcial (H5) | Envio, rascunho e evento **pelo navegador** (`src/integrations/google.js`, usado em `EnviarApresentacao.jsx`); envio e evento testados com mock, o rascunho sem teste. Nada no backend; ler o Gmail exige OAuth de autorização com `gmail.readonly`, que não existe. A tela de integrações ainda diz que as ações de Gmail e Agenda dependem de OAuth. Testes: `workflows.test.jsx` ("envia e-mail de verdade pela Gmail API com o token obtido", "cria evento de verdade na Google Agenda com datas no formato esperado", "recusa enviar e-mail real quando o Google não está configurado"). |
| Microsoft 365 | Não | n/a | Não registrado | Só a entrada de catálogo, declarada como dependência externa no hub de integrações. |
| LinkedIn oficial | Não | n/a | Não registrado | Catálogo e busca pública; não equivale à API. |
| monday.com | Parcial | Não | Não registrado | OAuth 2.1/PKCE, receptor de webhook (JWT) e inbox (0139) existem; **falta a projeção** dos eventos para registros do ERP (ficam `received`), e não há teste. Conta ainda não conectada. |
| Power BI | Não | n/a | Não registrado | Só a entrada de catálogo. |
| Hub de integrações (prontidão e teste por conector) | Sim | Sim | Não registrado | `handleTodoGreenIntegrations` (`todogreen-integrations.js`) em `/api/todogreen/integrations`: prontidão sem segredo, teste técnico por provedor e catálogo que declara Microsoft 365, LinkedIn, Power BI e OCPP como dependência externa. Testes: `todogreenIntegrationsService.test.js` ("mostra prontidão por conector sem expor credenciais", "SEFAZ com certificado mas SEM conector não conta como configurada", "recusa teste de provedor sem permissão de integração"), `IntegrationsPage.test.jsx` ("deixa o usuário conectar a própria IA e mostra o hub operacional completo"), e2e crítico `todogreen-navegacao.spec.js` ("integrações do TMS têm ação de verdade, não só selo"). |
| Geoapify (rotas, elevação e estimativa de tempo e distância) | Preparado | Sim | Não registrado | Adaptador `geoapify-provider.js` (perfil de rota por classe da frota), elevação e `POST /api/rotas/estimativa` (`route-estimate.js`, 10 por minuto por pessoa), dormentes até `GEOAPIFY_API_KEY`. Testes: `route-estimate.worker.test.js` ("sem chave do Geoapify, manda abrir no Maps em vez de usar serviço público proibido", "geocodifica e roteia só pelo Geoapify, com a chave no servidor"), `geoapify-provider.worker.test.js` ("normaliza GeoJSON da Geoapify para o contrato OSRM da tela", "mapeia classes da frota para perfis de rota seguros"). |

## Eletrificação, roteirização e viabilidade

Módulos de domínio puros, determinísticos e testados; a maioria já está ligada
ao servidor e à tela. O que ainda é só domínio, sem consumidor no servidor nem
nas telas, está marcado **Não** em Implementado, com o domínio testado.

| Processo | Implementado | Testado | Homologado | Evidência e fronteira |
| --- | --- | --- | --- | --- |
| Estimativa de energia/autonomia | Sim | Sim | Não registrado | `energyEstimationDomain.js`, usado pela viabilidade, pelo pré-flight e pelo `electric-plan`. Só a viabilidade enriquece a rota com elevação e clima com proveniência (0122; o clima vem da MET Norway, porque a API gratuita do Open-Meteo é só para uso não comercial; com `GEOAPIFY_API_KEY`, a elevação vem primeiro da Geoapify) — o pré-flight estima sem elevação nem clima, e o `electric-plan` usa o que vier no corpo. SOC ausente vira 100% sem registrar premissa. O baseline por veículo (0123) é devolvido pela API, mas ainda não substitui o consumo nominal. Testes: `energyEstimationDomain.test.js` ("recusa (invalid) sem distância, sem consumo ou sem bateria — nunca inventa", "sem elevação informada NÃO inventa: campos ficam null e registra a premissa"), `geoProvidersDomain.test.js` ("converte a série em UTC para a hora de Brasília que o modelo já lê"), `todogreen-geo-fleet.worker.test.js` ("com Geoapify calcula ganho/perda online e cacheia por 30 dias", "fonte fora do ar é WEATHER_NOT_AVAILABLE, não temperatura inventada"), `todogreen-viability.worker.test.js` ("v1 nasce com energia ESTIMADA no servidor, proveniência e sem faltas quando o custo existe"). |
| Tarifa de energia de referência (ANEEL) | Sim | Sim | Não registrado | `energyTariffDomain.js` (hierarquia contrato > informada > ANEEL > fallback) e cache 0124. Testes: `energyTariffDomain.test.js` ("contratual vence tudo e é INFORMED/HIGH", "nada disponível → tier null, curva vazia, sem número inventado"), `todogreen-energy.worker.test.js` ("ANEEL: só linhas de energia (MWh) entram, com data da fonte e vigência", "tarifa contratual informada vence a ANEEL e diz que é INFORMED"). |
| Preço de diesel de referência (ANP) | Sim | Sim | Não registrado | `anpDieselPriceDomain.js`; ingestão a cada 6 dias ou `POST /energy/anp/import`, desatualizado em 21 dias (0124). Testes: `anpDieselPriceDomain.test.js` ("marca stale quando o preço escolhido é velho"), `todogreen-energy.worker.test.js` ("importação manual do CSV da ANP pelo endpoint", "cron: só o que venceu roda; segunda rodada logo depois não chama nada"). |
| Janela energética de recarga (ONS) | Sim | Sim | Não registrado | `gridWindowDomain.js` (pesos 60/40), perfil 24 h por subsistema (0124). Sem fator de emissão horário — o próprio código diz que não é medição de emissões. Testes: `gridWindowDomain.test.js` ("sem ONS: financeira fora de ponta, recomendada = financeira, aviso ONS_NOT_AVAILABLE"), `todogreen-energy.worker.test.js` ("ONS: perfil de 24 h por subsistema com último instante da fonte"). |
| Plano de recarga por veículo | Sim | Sim | Não registrado | `planoDeRecargaPorVeiculo` no `/energy/plan`, em slots de 1 h; não envia comando OCPP. Testes: `smartChargingPlan.test.js` ("respeita a demanda contratada: reduz potência e avisa", "conector incompatível não é alocado e diz por quê"), `smartChargingDomain.test.js` ("com potência, entrega janela, horas, economia e demanda"), `todogreen-energy.worker.test.js` ("tarifa ANEEL vigente com curva por posto, janela energética do ONS, diesel municipal da ANP e plano por veículo dentro de 20h→6h"). |
| Radar de mercado estruturado (PNCP · Compras.gov · GDELT) | Sim | Sim | Não registrado | `marketSignalDomain.js`, `todogreen-market-signals.js` (0125, preferências 0133), no cron; "Criar oportunidade" a partir do sinal exige `crm:manage`. Testes: `marketSignalDomain.test.js` ("o mesmo processo visto no PNCP e no Compras.gov tem o mesmo fingerprint (dedupe entre fontes)"), `todogreen-market-signals.worker.test.js` ("PNCP: normaliza, pontua, rejeita fora de escopo e deduplica entre termos", "POST :id/opportunity cria a oportunidade pela esteira de records, marca a triagem e é idempotente; exige crm:manage"). |
| Risk Map (PRF · ANTT) | Sim | Sim | Não registrado | `roadRiskDomain.js`, `todogreen-road-risk.js` (0125); sem índice é `RISK_DATA_NOT_AVAILABLE`, nunca zero. A PRF exige importação manual. Testes: `roadRiskDomain.test.js` ("sem índice ingerido o score é null (RISK_DATA_NOT_AVAILABLE), não zero"), `todogreen-road-risk.worker.test.js` ("o risco da rota é null com motivo, nunca zero; status diz o que falta", "a rota que atravessa as células recebe score, trechos críticos e confiança", "importação pelo endpoint exige permissão e CSV válido"). |
| Alternativas de rota com risco como custo | Parcial | Sim | Não registrado | Conectadas no OSRM (`alternatives=true`) e no Valhalla (`alternates`), com ranking por custo, risco e pedágio (`todogreen-routing-maps.js`). **No caminho Geoapify — o primário quando a chave existe — há uma rota só, sem ranking** (o risco ainda é calculado para ela); o ranking marca toda rota como compatível com as restrições, fixo; o ramo Valhalla com alternativas não tem teste. Testes: `todogreen-road-risk.worker.test.js` ("OSRM com alternatives=true devolve as duas rotas com refs; o ranking usa o risco", "sem alternatives o formato antigo continua: uma rota, sem ranking", "conta praças por alternativa, converte em custo com a tarifa informada e o ranking muda"), `routeAlternativesDomain.test.js` ("risco NÃO bloqueia: a rota arriscada A continua disponível e listada"). |
| Design system — tokens e regressão visual | Sim | Parcial | n/a | Bloco canônico `.tdg` em `LogisticsVertical.css` e `src/design-system/tokens.css`. Só a guarda de tokens roda no gate: a regressão visual (`e2e/visual/`, `npm run test:visual`) fica fora do `test:e2e:critical`. Baselines gerados na sessão remota; o caminho Docker não foi exercitado. Testes: `designTokens.test.js` ("nenhum token é declarado duas vezes no mesmo arquivo e seletor", "todo token usado tem definição (nada vale só pelo fallback)", "o bloco canônico da vertical existe e cobre o escuro"). |
| Green On — pontos de recarga e OCPP | Parcial | Sim | Não registrado | Pontos (0114), sessões, reservas com trava de conflito e preço por kWh (0130), coleções D1 da vertical (`vertical-records/colecoes/energia.js`); a cobrança de recarga por kWh é derivada no navegador, sem título, razão nem documento fiscal. OCPP só no catálogo — nenhum comando sai do sistema; a sessão nasce `manual` por padrão, mas o corpo pode gravar a fonte OCPP sem integração que confirme ([O7](#achados-de-leitura-ainda-não-reproduzidos)). Até 24/09 um PATCH do preço por kWh sem `campos` apagava os campos livres; corrigido neste PR. Testes: `todogreen-vertical-records.worker.test.js` ("grava o ponto e deriva 'serve pesado' de corrente + potência", "cria sessão concluída somente com energia medida", "barra reservas cruzadas no mesmo ponto e permite fronteira contígua", "salva preço por kWh e preserva a precedência de escopo no domínio"), `verticalRecordsContract.test.js` ("%s: PATCH parcial não reescreve o que não mudou — colunas(daLinha(linha)) reproduz a linha"), `chargingBillingDomain.test.js` ("sem preço aplicável, não fatura (não cobra R$ 0)"). |
| GreenPay — razão e repasse | Sim (razão) · Preparado (repasse) | Sim | Não registrado | Razão interno e contratos de ganho (0113/0131); repasse PIX via SysPag dormente sem `SYSPAG_API_TOKEN` **e** `SYSPAG_BASE_URL` — "pago" é só o razão interno. Testes: `todogreen-greenpay.worker.test.js` ("a régua expõe a prontidão SysPag DORMENTE por padrão, sem vazar segredo", "sincronizar deriva o ganho das entregas já feitas (idempotente)", "aprovar move pendente → aprovado; pagar move aprovado → pago", "concilia lote pago com retorno externo sem alterar o razão"), `syspagDomain.test.js` ("só token (sem URL) ainda não habilita"). |
| Core All Green / Greenmob | Parcial | Parcial | Não registrado | As verticais de `src/features/greenmob/` e `src/features/greenon/` (CRM, locação, notícias) gravam só no blob do workspace, sem backend; os pontos, sessões e preços de recarga Green On são coleções D1 da vertical To Do Green. O `tenant_id` é único (`todogreen`). O menu mostra as verticais irmãs só com vínculo confirmado por `/api/todogreen/access`, com link de volta ao núcleo. Testes: `greenmobRentalDomain.test.js` ("MRR soma apenas contratos ativos (entregue, ativo, renovado)"), `greenOnCrmDomain.test.js` ("o resumo do funil separa abertas de ganhas/perdidas e soma CAPEX e forecast"), `VerticalShortcuts.test.jsx` ("com vínculo confirmado, leva às três verticais", "sem vínculo, o menu fica como era"), `LogisticsVertical.test.jsx` ("a barra leva às verticais irmãs e de volta ao núcleo All Green"). |
| Seleção de motor OSRM×Valhalla | Sim | Sim | Não registrado | `routingEngineSelectionDomain.js`, `routing-providers.js`; pesado sem Valhalla → `NO_SAFE_ROUTING_ENGINE` (409). Com Geoapify, a rota vai pela Geoapify antes do seletor, com perfil por classe (toco `truck`, 3/4 `medium_truck`, van e VUC `light_truck`). **Fronteira:** veículo leve sem Geoapify nem OSRM próprio cai no OSRM público de demonstração, que o `AGENTS.md` proíbe (ver também [O6](#achados-de-leitura-ainda-não-reproduzidos)). Testes: `todogreen-routing-providers.worker.test.js` ("pesado sem Valhalla → 409 NO_SAFE_ROUTING_ENGINE, sem chamar motor nenhum", "Valhalla configurado mas fora do ar: pesado NÃO cai em OSRM — falha explícita"), `routingEngineSelectionDomain.test.js` ("pesado com Valhalla FORA do ar NÃO cai para OSRM — sem motor seguro"). |
| Restrições viárias (OSM) | Não | Parcial | Não registrado | Só o domínio (`roadRestrictionDomain.js`), sem consumidor no worker nem nas telas — o ranking de alternativas trata toda rota como compatível; depende do grafo OSM/PostGIS. Só teste de domínio: `roadRestrictionDomain.test.js` ("altura: caminhão de 4,2 m barrado em ponte de 3,5 m", "escolhe a rota mais curta COMPATÍVEL e registra por que a mais curta foi rejeitada"). |
| Snapshot de viabilidade | Sim | Sim | Não registrado | Append-only pela API (sem trigger no D1) e versionado (0121), com gate server-side da proposta ligada a oportunidade. Testes: `viabilitySnapshotDomain.test.js` ("conteúdo idêntico NÃO cria versão nova (idempotente)", "snapshot ausente é bloqueio explícito"), `todogreen-viability.worker.test.js` ("mudança real de premissa cria v2 encadeada ao hash anterior — v1 permanece", "não existe caminho de alteração ou exclusão", "liberar (sent) sem snapshot é recusado no servidor") e o passo 4b da jornada. |
| Pré-flight operacional | Sim | Sim | Não registrado | Persistido e ligado à rota (0132); o despacho automático passa pelo mesmo gate. Sem SOC informado nem telemetria, presume 100% de SOC e de SoH — registra `SOC_PRESUMIDO_100` na proveniência, sem rebaixar o status ([O9](#achados-de-leitura-ainda-não-reproduzidos)); o gate pode ser desligado por `TDG_PREFLIGHT_GATE_DISABLED=1`. Testes: `preflightDomain.test.js` ("rota longa sem recarga disponível → BLOCK com sugestões", "sem veículo da frota → WARNING vehicle_unknown (nunca PASS por omissão)"), `todogreen-preflight.worker.test.js` ("motorista e veículo em dia, rota curta → PASS persistido, com energia e proveniência", "pré-flight BLOCK → 409; pré-flight de OUTRA rota (paradas diferentes) → 409", "kill switch TDG_PREFLIGHT_GATE_DISABLED=1 deixa passar, mas sem carimbar status verificado"), `todogreen-dispatch.worker.test.js` ("não aplica rota quando o pré-flight retorna BLOCK", "bloqueia o formato legado de atribuição direta sem rota/pré-flight"). |
| Action Queue | Sim | Sim | Não registrado | BLOCK/WARNING e risco alto viram itens na Torre de Controle, com dedupe. O risco que vira ação é o que a tela manda (o servidor não recalcula), e esse caso testa a função pura, não a rota HTTP. Sem fechamento automático do item. Testes: `todogreen-preflight.worker.test.js` ("BLOCK/WARNING viram itens no quadro seed, sem duplicar na repetição do mesmo par", "risco viário alto do traçado vira ação; PASS sem risco não gera nada"). |
| Proveniência de dados | Parcial | Sim | Não registrado | `dataProvenanceDomain.js` usado no worker (pré-flight, viabilidade) e nos domínios de tarifa, diesel, ONS, clima e baseline; na tela, só a data e o "desatualizado" da tarifa e do diesel. Testes: `dataProvenanceDomain.test.js` ("dado ausente NÃO é confiável: value null força confiança UNKNOWN", "medição ganha de estimativa mesmo com confiança igual"), `todogreen-preflight.worker.test.js` ("motorista e veículo em dia, rota curta → PASS persistido, com energia e proveniência"). |
| Perfil físico/energético do veículo | Sim | Sim | Não registrado | Colunas da 0123 gravadas pelo cadastro da frota na criação e na edição (a importação em massa não grava). Testes: `todogreen-geo-fleet.worker.test.js` ("o perfil físico é gravado na criação e devolvido no mapeamento", "PATCH atualiza o perfil junto do cadastro"). |
| Fila offline do motorista | Sim | Sim | Não registrado | `driverOfflineQueueDomain.js` consumido pelo portal, com idempotência no servidor (0094). Testes: `driverOfflineQueueDomain.test.js` ("um clique repetido (mesma chave) colapsa num só, preservando o primeiro"), `todogreen-driver-portal.worker.test.js` ("reenvio com a mesma chave devolve o evento já gravado (200) e não duplica", "reenvio com a mesma chave NÃO guarda a imagem de novo (dedup antes de gravar)"). |
| Geocodificação e traçado da vertical (gateway de mapas) | Parcial | Sim | Não registrado | `todogreen-routing-maps.js` atrás de sessão (`/api/todogreen/maps/geocode` e `/maps/route`): Geoapify primeiro quando há chave; depois Nominatim, OSRM e Valhalla próprios e, sem eles, os públicos. **Lacuna:** a tela só usa o gateway quando há token no navegador; sem ele chama direto o Nominatim público e o OSRM de demonstração, e o botão de distância da Precificação nunca envia o cabeçalho ([O6](#achados-de-leitura-ainda-não-reproduzidos)). Testes: `todogreen-routing-maps.worker.test.js` ("prefere Nominatim próprio e preserva a busca no Brasil", "usa serviço público somente como fallback quando o host próprio cai"), `distanciaRodoviariaDomain.test.js` ("usa o gateway interno quando há sessão autenticada"). |
| Planejamento de recarga na rota (`electric-plan`) | Sim | Sim | Não registrado | `planElectricRoute` só na API externa `POST /api/tms/v1/routes/electric-plan` (chave com `routing:write`): plano, estimativa de energia, seleção de motor (metadado) e pré-flight; não persiste, e nenhuma tela chama. O teste de unidade do planejador (`todogreen-electric-routing.test.js`) fica fora do gate. Testes: `todogreen-routing-api.worker.test.js` ("planeja rota elétrica e anexa a estimativa de energia estruturada e versionada", "anexa o pré-flight PASS quando a rota fecha sem recarga", "pré-flight BLOQUEIA e sugere solução quando a autonomia não fecha"). |
| Otimização de rotas (VROOM auto-hospedado) | Preparado | Sim | Não registrado | `optimizeTodoGreenRouting` em `POST /api/todogreen/routing/optimize` (sessão) e `/api/tms/v1/routes/optimize` (chave `routing:write`); sem `TDG_ROUTING_URL` responde 503, o Roteirizador cai na ordenação local e o despacho, no solver WASM. Testes: `todogreen-routing-api.worker.test.js` ("retorna 503 quando o motor gratuito ainda não foi conectado", "reaproveita o mesmo VROOM na rota interna sem exigir chave TMS", "encaminha o problema ao VROOM auto-hospedado e preserva o resultado"). |

## Módulos que não estavam na matriz

Existem no código e não constavam da versão anterior.

| Módulo | Implementado | Testado | Homologado | Evidência |
| --- | --- | --- | --- | --- |
| Portal do colaborador (PJ e CLT) e NF do PJ | Sim (pagamento: Preparado) | Sim | Não registrado | `todogreen-employee-portal.js`, `pjInvoiceDomain.js`, 0117; rota `/portal-colaborador`. Sem SysPag, "pagar" marca a nota paga e quita a conta a pagar só no razão interno; aprovar e pagar gravam no razão sem a trava de competência (L3). Testes: `todogreen-employee-portal.worker.test.js` ("PJ resolve pelo e-mail, mostra o esperado e pode imputar", "CLT vê os próprios dados mas NÃO imputa nem informa PIX", "pagar dispara o repasse (SysPag dormente) e quita a conta a pagar", "cada PJ só vê as próprias notas"), `pjInvoiceDomain.test.js` ("aprovar/recusar só em análise; pagar só aprovada; reenviar em análise/recusada"). |
| Chamados do colaborador | Sim | Sim | Não registrado | `employeeTicketDomain.js`, 0118, no portal do colaborador. Testes: `todogreen-employee-portal.worker.test.js` ("CLT abre chamado de divergência e o vê aberto na própria sessão", "a equipe atende, resolve com resposta, e o colaborador vê a resposta", "cada colaborador só vê os próprios chamados"), `employeeTicketDomain.test.js` ("ativo, atender, resolver, cancelar por status"). |
| Chave PIX do motorista e adaptador SysPag | Sim (repasse: Preparado) | Sim | Não registrado | 0116, `pixDomain.js`, `syspagDomain.js`, `todogreen-syspag.js`; o adaptador só liga com token **e** URL, e o disparo real não tem teste. Testes: `todogreen-driver-portal.worker.test.js` ("grava a PRÓPRIA chave (validada e normalizada) e a sessão passa a mostrá-la", "chave malformada é recusada (400) e nada é gravado"), `pixDomain.test.js` ("CPF válido passa; inválido e sequência são recusados"), `syspagDomain.test.js` ("sem token nem URL, não habilita e lista o que falta"). |
| Portal TMS, projeções TRACK3R, TMS manual e ponte local | Sim (ponte local: Preparado) | Parcial | Parcial (H3) | `TmsPortal.jsx` (`/portal-tms`), `todogreen-track3r-projectors.js` (0135–0138; as recusas da 0142 são do receptor), `todogreen-tms-manual.js` e `todogreen-tms-local-bridge.js`, que exige `TODOGREEN_TRACK3R_LOCAL_BRIDGE_SECRET` (sem ele, 401) e não tem teste. O projetor casa o cliente só pelos dígitos do CNPJ ([O5](#achados-de-leitura-ainda-não-reproduzidos)). Testes: `todogreen-tms-webhooks-all.worker.test.js` ("projeta lista e encomenda no TMS independentemente da ordem de chegada", "projeta fatura de cliente no contas a receber e no razão", "projeta faturas de motorista e rede terceira como custos sem duplicar reenvio"), `todogreen-tms-manual.worker.test.js` ("com contrato aprovado e assinado, cria a OS e ela aparece na lista de transações", "motorista não acessa o cadastro manual do TMS"), `todogreen-tms.worker.test.js` ("lista os três tipos com contadores por tipo"), e2e crítico `todogreen-navegacao.spec.js` ("a Torre abre pelo clique, volta ao ERP e sobrevive ao F5"). |
| Central RFQ/RFI (acervo de habilitação) | Sim | Sim | Não registrado | `habilitacaoDomain.js`, 0083, `CentralRfqPage.jsx`; o status é fórmula, nunca coluna. Testes: `todogreen-vertical-records.worker.test.js` ("a dona cadastra documento e ele volta sem coluna de status", "vendedora LÊ o acervo para responder RFQ mas não cadastra documento oficial", "o acervo de um espaço não vaza para o outro", "RFQ fechado sem motivo é recusado"), `habilitacaoDomain.test.js` ("essencial vencido TRAVA o envio — mandar assim é pior que não mandar"), `CentralRfqPage.test.jsx` ("quem não tem compliance:manage lê e não cadastra"). |
| Pastas do cofre | Sim | Sim | Não registrado | `pastasDomain.js`, 0084; visibilidade pela linhagem. Testes: `todogreen-vertical-records.worker.test.js` ("subpasta 'do espaço' dentro de uma privada NÃO vaza para quem não vê o pai", "pasta não entra dentro da própria descendência"), `todogreen-file-vault.worker.test.js` ("quem não vê a pasta não vê o arquivo na lista — nem o da subpasta 'do espaço'"), `pastasDomain.test.js` ("A REGRA QUE MAIS IMPORTA: subpasta do espaço dentro de privada continua invisível"). |
| "Sobre o negócio", dossiê da IA e Plantû | Sim | Sim | Não registrado | `businessContextDomain.js`, 0082, `todogreen-semente.js`, 0091–0093 e 0095. Testes: `todogreen-vertical-records.worker.test.js` ("a dona cadastra um ponto e ele volta na leitura", "vendedora LÊ o dossiê mas não ensina — ensinar muda o que a IA afirma para todo mundo", "o dossiê de um espaço não vaza para o outro"), `todogreen-semente-memoria.worker.test.js` ("não vaza a thread de uma pessoa para outra (isolamento por espaço/usuário)", "turno montado com dado RESTRITO no contexto só volta para quem tem finance:manage"), `todogreen-semente-oportunidade.worker.test.js` ("NÃO fecha negócio pelo chat: ganha/perdida é da tela do CRM (400)"), `businessContextDomain.test.js` ("o restrito só entra quando quem pergunta pode ver"). |
| Automações configuráveis da Central | Sim | Sim | Não registrado | 0049, `runTodoGreenScheduledWorkAutomations` (`todogreen-work-center.js`) no cron. Testes: `todogreen-work-automations.worker.test.js` ("executa a regra no servidor quando o status muda", "processa prazo vencido pelo cron sem abrir o cartão", "vincula a conta do CRM e exige confirmação humana antes do WhatsApp"). |
| Radar por busca web e inteligência de mercado/conta | Sim | Parcial | Não registrado | `todogreen-market-radar.js` é busca ao vivo, sem gravar; `todogreen-market-intelligence.js` (0075) e `todogreen-client-intelligence.js` (acompanhamentos em `todogreen_intelligence_watches`, 0052), este sem teste de worker. Testes: `todogreen-market-intelligence.worker.test.js` ("pesquisa, preserva a fonte e isola os resultados por workspace", "exige empresa para possíveis decisores e permissão para pesquisar"), `todogreen-market-signals.worker.test.js` ("o radar por busca web está roteado (regressão: a tela chamava um endpoint sem rota)"), `marketRadar.test.js` ("aceita processo aberto de transporte e rejeita vaga"). |
| Acesso: pedidos, convites e senha provisória | Sim | Sim | Parcial (H4) | Pedidos de acesso (`0086_todogreen_access_requests.sql`) e convites (`0088_todogreen_access_invites.sql`): o convite vale pelo link, com ou sem Brevo, e o Painel de Acessos mostra o link (a exibição não tem teste). A senha provisória (`users.must_change_password`, 0143) é do app geral (tela Equipe); no ERP o caminho é o convite. Com as duas chaves do Turnstile, o pedido de acesso e o login também exigem o token anti-robô. Até 24/09 `compliance:manage` e `business:teach` não podiam ser dados num acesso sob medida; corrigido neste PR. H4 cobre só a resolução de papel, com vínculos inseridos direto no banco. Testes: `todogreen-access.worker.test.js` ("ter e-mail do domínio da empresa não dá acesso a nada", "pedir o espaço de outra pessoa é recusado"), `todogreen-access-requests.worker.test.js` ("aceita o pedido público sem sessão e não duplica o pendente", "aprovar concede acesso de verdade e trava o segundo julgamento", "sem e-mail configurado, aprovar devolve um link de convite válido para entrega manual"), `todogreen-access-binding.worker.test.js` ("jurídico/compliance e o dossiê do assistente podem ser dados sob medida", "encerra as duas fontes de vínculo, não só a liberação por e-mail"), `temp-password-access.worker.test.js` ("cria a conta, obriga a trocar no primeiro acesso e libera depois"), `authFlows.test.jsx` ("recupera a senha pelo código e entra direto na vertical"). |
| Solicitações do cliente, atendimento automatizado e NPS | Sim | Sim | Não registrado | `todogreen-requests.js` (0040), rotas do portal (`customer-portal/rotas/`), `atendimentoAutomatizadoDomain.js` e NPS (0115); por encomenda (0144), devolução e alteração de endereço antes da entrega e acareação depois, só em operação do próprio cliente. Testes: `todogreen-client-requests.worker.test.js` ("o gestor do cliente abre e o pedido nasce com prazo", "o cliente B não enxerga o pedido do cliente A por nenhum parâmetro", "nota interna da equipe não sai no portal do cliente"), `todogreen-requests-internal.worker.test.js` ("o vendedor vê só a própria carteira"), `todogreen-customer-portal.worker.test.js` ("pedido de nova rota vira chamado da equipe, nunca resposta de IA", "em trânsito: aceita devolução e alteração de endereço", "entregue: aceita acareação e recusa devolução"), `todogreen-portal-nps.worker.test.js` ("nota de detrator abre ocorrência com prazo na fila da equipe"). |
| Painel comercial (retrato de receita) | Sim | Parcial | Não registrado | `todogreen-commercial-panel.js` (0140/0141). Só teste de unidade: `commercialPanelDomain.test.js` ("tudo vazio: cada seção marca disponivel:false, sem número inventado", "soma faturas lançadas depois do corte por cima da base, preservando o histórico"). |
| Dashboards configuráveis | Sim | Sim | Não registrado | `todogreen-dashboards.js` (0039). Testes: `todogreen-dashboards.worker.test.js` ("vendedor cria painel pessoal com indicadores validados", "impede métrica inventada", "somente administrador cria painel visível para a equipe"), `DashboardBuilderPage.test.jsx` ("carrega painéis pessoais e mostra a prévia dos indicadores"). |
| Desempenho de preço (previsto × realizado) | Sim | Parcial | Não registrado | `todogreen-pricing-performance.js` (0051). Só teste de unidade: `pricingPerformanceDomain.test.js` ("abre margem e desvios sem esconder o cálculo", "não recomenda mudança da régua com amostra insuficiente"). |
| Motor HC + DRE | Sim | Parcial | Não registrado | `todogreen-operation-params.js` (0099), `operationEngineDomain.js`. Só teste de unidade: `operationEngineDomain.test.js` ("reproduz o 1.3252 da régua da titular", "desconta custo, overhead, comissão e impostos da receita"). |
| Vistoria, jornada, turnos, CNH e score do motorista | Sim | Sim | Não registrado | 0097, 0109, 0110; `driverChecklistDomain.js`, `driverJourneyDomain.js`, `driverScoreDomain.js`. O envio da foto da CNH não tem teste. Testes: `todogreen-driver-portal.worker.test.js` ("vistoria completa e conforme → 201 aprovado e apto", "problema em item crítico → reprovado, com o crítico contado", "inicia o turno, recusa iniciar de novo e encerra com horas derivadas", "com cadastro, resolve pelo e-mail e avisa CNH a vencer"), `todogreen-driver-score.worker.test.js` ("a comparação é ANÔNIMA: nunca vaza nome, id ou e-mail de outro motorista"). |
| Rota do dia, roteirizador e despacho inteligente | Sim | Sim | Não registrado | `todogreen_routes` (0100), 0106, `todogreen-dispatch.js`, `worker/vrp/`; o e2e `todogreen-roteirizacao.spec.js` fica fora do gate. Até 24/09 a leitura das rotas dava 500 para planejamento e auditor; corrigido neste PR. Testes: `todogreen-dispatch.worker.test.js` ("cria a rota, liga as operações e ocupa os recursos em um único plano", "reaplicar o mesmo plano é idempotente"), `todogreen-dispatch-vroom.worker.test.js` ("usa peso, volume, pallets e pacotes como capacidade multidimensional"), `todogreen-driver-portal.worker.test.js` ("a operação cria a rota e o motorista dono a enxerga; o outro não"), `todogreen-vertical-records.worker.test.js` ("planejamento e auditor do espaço leem as rotas"). |
| Pedágios, carregadores públicos, CEP e dados abertos | Sim | Parcial | Não registrado | `todogreen-pedagios.js`, `todogreen-carregadores.js`, `todogreen-integration-gateway.js`. Só o CEP tem teste de worker; o pedágio entra no teste de risco com a função injetada, e `/api/todogreen/carregadores` não tem teste no gate. Testes: `todogreen-integration-gateway.worker.test.js` ("consulta CEP pelo backend e rejeita CEP inválido", "consulta CEP com fallback: pula o provedor que falha e usa o próximo", "não libera Open Charge Map sem chave"), `pedagiosDomain.test.js` ("casa praças a até 1,5 km da rota, deduplica e ordena pela viagem"). |
| Conexões MCP | Sim | Parcial | Não registrado | `worker/services/mcp-connections.js`; o teste só cobre a guarda de URL — o handler (`integration:manage`, grava cifrado) não é testado. Teste: `mcp-connections.worker.test.js` ("bloqueia endpoint inseguro ou privado: %s"). |
| Caixa de entrada por e-mail (Brevo Inbound) | Sim | Sim | Não registrado | Transversal, do app geral: `worker/services/inbound-webhooks.js` numa rota pública, com `worker/mensageria/inbound-email.js` normalizando o conteúdo. Testes: `inbound.worker.test.js` ("recebe e-mail por webhook e roteia pelo destinatário", "recebe e-mail no formato Brevo (items[]) com segredo pela URL", "recusa o webhook de e-mail quando o segredo está errado"), `inbound-email.test.js` ("normaliza o formato Brevo (items[]) e descarta mensagem sem destinatário"). |
| Extensão do navegador (Plantû em qualquer página) | Sim | Parcial | Não registrado | `extension/`, empacotada em `/extensao-todogreen.zip` (`scripts/extension-package.js`); o popup manda a página para `POST /api/todogreen/semente` com o token da sessão e só executa ações depois do clique; download e token em Meu perfil. O popup não tem teste; instalação manual, fora de loja. Testes: `extension-manifest.test.js` ("tem a pasta de idioma que o default_locale exige"), `extension-package.test.js` ("o build publica o pacote no endereço que a tela usa", "a validade do token dita na tela é a mesma da sessão no servidor"), `TodoGreenProfile.test.jsx` ("mostra o pacote para baixar e o token mascarado"). |
| Itens menores | Sim | Parcial | Não registrado | Modelos de rota recorrente (0119), avisos de pendência (0101, sem teste), fechamento ESG mensal (0108), régua ambiental (0103). Testes: `todogreen-vertical-records.worker.test.js` ("cria, lista e devolve o texto das paradas, isolado por espaço"), `todogreen-esg-regua.worker.test.js` ("fecha o mês: consolida cálculos + atividade tonne-km, e não duplica ao refechar"). |
| Telas simuladoras (sem API nem persistência) | Não | Parcial (domínio) | n/a | Green On App, Roaming (OCPI), console OCPP, conta corporativa Green On, BESS/pico, fila de alertas, cobrança SaaS, acessos multi-tenant, grupo de entidades, segurança física, RASCI e locação Greenmob — calculam ou demonstram, sem efeito de negócio no servidor. Os domínios têm teste (o RASCI é tabela estática), por exemplo `ocpiRoamingDomain.test.js` ("validadores OCPI aceitam formatos válidos e recusam inválidos"), `ocppProtocolDomain.test.js` ("encodeCall/parseMessage é round-trip"), `saasBillingDomain.test.js` ("hasModulo é o predicado único de contratação"). |

## Lacunas de controle

Controles que a versão anterior da matriz dava como fechados — ou que esta
revisão encontrou — e que o código permite (ou permitia) contornar. Todas foram
**reproduzidas em 24/09/2026** por requisição HTTP contra o Worker local, com os
papéis citados; L11 e L12 são demonstradas pelos próprios testes do gate. As
corrigidas têm teste de regressão que falhava antes da correção.

| Código | Lacuna | Onde | Situação |
| --- | --- | --- | --- |
| **L1** | O status do documento jurídico é gravado direto pela coleção genérica `legal` (`POST`/`PATCH /api/todogreen/records/legal`), que só exige `proposal:manage` — permissão do vendedor. Isso satisfaz `juridicoConcluido` sem passar pela máquina de estados (`registrarEventoJuridico`) nem por `compliance:manage`. A Central Jurídica (`tdgLegalBridge.js`/`useTdgLegalRecords.js`) também grava `situacao` direto. | coleção `legal` em `worker/services/vertical-records/colecoes/juridico.js` | **Aberta.** Reproduzida: um vendedor grava o documento já `aprovado` com `campos.contractId`, e o contrato passa pela aprovação (200). O teste de vertical-records também cria o documento já `aprovado`. |
| **L2** | O gate de assinatura confere só a **existência** de um anexo com contexto jurídico — aceita inclusive referência externa sem bytes — e o upload exige só `proposal:manage`. | `documentoDeAssinaturaVinculado` / `temAnexoNoCofre` em `vertical-records/gates.js`; `createReference` em `todogreen-file-vault.js` | **Aberta.** Reproduzida: uma referência externa de 0 byte no cofre, com contexto jurídico, satisfaz o gate. Somada à L1, um vendedor leva o contrato sozinho a aprovado e assinado. |
| **L3** | Folha (fechamento e reprocessamento, que arquiva lançamentos não pagos), férias, rescisão, recebimento de compras e aprovação e pagamento da NF do PJ gravam ou alteram `todogreen_financial_entries` sem consultar a trava de competência fechada; pela leitura, também as projeções financeiras do TRACK3R e o cancelamento de CT-e/NFS-e com fatura. | `todogreen-payroll.js`, `todogreen-purchasing.js`, `todogreen-employee-portal.js`, `todogreen-track3r-projectors.js`, `todogreen-fiscal.js` | **Aberta.** Reproduzida em três caminhos: fechar a folha, receber a compra e aprovar a NF do PJ lançam no razão de um mês fechado. Os demais vêm da leitura: só `todogreen-transactions.js`, `vertical-records/gates.js` e `todogreen-treasury.js` leem `todogreen_financial_periods`. |
| **L4** | A alçada de compras era contornável pelo corpo do pedido: (a) o portão comparava o `status` cru e o serviço de compras apara — `"aprovada "` passava como edição comum e chegava como aprovação, sem faixas nem segregação; (b) a trilha `campos.purchaseApprovalFlow` era aceita do cliente, na criação ou numa edição (inclusive no reenvio do requisitante, que não tem `purchase:manage`), com todas as etapas "aprovadas"; (c) o clique de aprovar podia trocar as linhas: a alçada avaliava R$ 100 gravados e o pedido saía aprovado com R$ 500 mil e `aprovacaoValida: true`. | `todogreen-purchasing-enterprise.js` × `todogreen-purchasing.js` | **Corrigida.** O portão lê o status com o mesmo leitor do serviço (`statusDoCorpo`), é o único que escreve a trilha, e a aprovação só leva a decisão (`status`, `revision`, `notaDecisao`). Testes: `todogreen-purchasing.worker.test.js` ("status com espaço recebe a mesma alçada do status exato", "trilha de aprovação enviada na criação não é gravada", "trilha enviada numa edição não vale, e a verdadeira sobrevive à edição", "editar o pedido não grava trilha de aprovação", "aprovar não troca os itens do pedido no mesmo clique"). |
| **L5** | O PATCH do contrato aceitava trocar `propostaId`/`clientId` sem repetir a checagem de proposta aceita e cliente coerente, que só rodava na criação. | `worker/services/vertical-records/crud.js` | **Corrigida.** Reproduzida: o PATCH trocava `propostaId` por uma proposta em rascunho e `clientId` por outro cliente (200). A criação e o PATCH que troca o par usam a mesma checagem (`conferirParDoContrato`), e a linhagem (oportunidade, cenário) acompanha a proposta nova. Teste: `todogreen-vertical-records.worker.test.js` ("o PATCH repete a checagem da criação quando proposta ou cliente mudam"). |
| **L6** | Nos fluxos de Qualidade, Marketing e processos gerais, o PATCH aceita `status` arbitrário sem passar pelas etapas de aprovação, e a criação aceita `requireApproval: false`. O mesmo vale para o domínio `legal` do fluxo empresarial, sem efeito no gate do contrato, que lê as aprovações. | `todogreen-enterprise-workflows.js` | **Aberta.** Reproduzida: `PATCH` com `status: approved` conclui o fluxo sem etapa, e a criação aceita `requireApproval: false`. |
| **L7** | A simulação oficial (`POST /api/todogreen/simulate` com `persist: true`) gravava cenário em `pricing_scenarios` para qualquer papel da vertical, enquanto a coleção de simulações e o Deal Desk exigem `pricing:simulate`. O cenário salvo entra no painel comercial e pode embasar um pedido ao Deal Desk. | `todogreen-core.js` | **Corrigida.** Reproduzida com um usuário de RH (200 e linha gravada); salvar agora exige `pricing:simulate`. Testes: `todogreen-simulate.worker.test.js` ("quem não simula preço não salva cenário", "quem simula preço salva o cenário"). |
| **L8** | Leitura sem permissão: os GET da tesouraria (saldos e extrato), das transações (OS, fila de faturamento, títulos e custos), das compras (requisições e pedidos) e do estoque só conferem o vínculo com o espaço — o roteador barra apenas o motorista. O colaborador PJ/CLT (`colaborador:self`, sem `read`) lê tudo isso, enquanto ler o razão pela coleção `financial` exige `finance:manage` ou `audit:read`. | `todogreen-treasury.js`, `todogreen-transactions.js`, `todogreen-purchasing.js`, `todogreen-stock.js` | **Aberta.** Reproduzida com um colaborador só com `colaborador:self`: saldos da tesouraria, títulos a receber (com dados), fila de faturamento, pedidos, requisições e saldos de estoque responderam 200; `/records/financial` respondeu 403. |
| **L9** | Pedido de compra editado depois de aprovado continua `aprovado`: a edição só marca `aprovacaoValida: false`, que nada consulta; a transição para `enviado` e o recebimento conferem só o status, e a tela oferece Enviar e Receber. | `todogreen-purchasing.js`, `purchaseDomain.js`, `pages/PurchasingPage.jsx` | **Aberta.** Reproduzida: pedido aprovado com R$ 100, editado para R$ 500 mil (`aprovacaoValida: false`, status `aprovado`) e enviado (200). O recebimento não foi exercitado; pela leitura, ele confere só o status. |
| **L10** | A segregação da requisição é contornável pelo corpo: o `requisitanteId` vem do corpo na criação e na edição, e o portão compara o aprovador com ele antes de `created_by`. Quem cria a requisição em nome de outra pessoa faz a primeira aprovação — na régua de fábrica, até R$ 5 mil, a única. | `todogreen-purchasing.js` × `todogreen-purchasing-enterprise.js` | **Aberta.** Reproduzida: um comprador com `purchase:manage` criou a requisição com outro requisitante e a aprovou ele mesmo (200). O pedido de compra não é afetado, porque usa `created_by`. |
| **L11** | O cenário salvo pela tela vem pronto do navegador: `/api/todogreen/records/scenarios` grava resultado, aprovações e premissas do corpo sem recalcular (até o id pode vir do corpo), e o Deal Desk calcula a alçada sobre esse resultado. O motor do servidor (`/simulate`) não é chamado por nenhuma tela. | `worker/services/vertical-records/cenarios.js`, `todogreen-deal-desk.js`, `journeys/PricingPanel.jsx` | **Aberta.** Demonstrada pelo próprio teste do gate: `todogreen-vertical-records.worker.test.js` ("salva com a procedência das premissas junto") grava o resultado enviado no corpo. |
| **L12** | A proposta aceita `cenarioId` inexistente ou de outro cliente: só se confere que o campo veio preenchido. Como a trava do Deal Desk é por `cenarioId`, um id inexistente não tem pedido e é liberado. | `worker/services/vertical-records/colecoes/comercial.js` | **Aberta.** Demonstrada pelo próprio teste do gate: `todogreen-vertical-records.worker.test.js` ("com cenário, salva") cria a proposta, já aceita, com um cenário que nunca foi semeado. |

As abertas mudam comportamento de telas (a Central Jurídica, por exemplo, grava
o status direto; a Precificação calcula no navegador) ou pedem decisão de
política (quem pode lançar em mês fechado, quem lê o financeiro); cada uma pede
o próprio PR, com teste que reproduza o contorno antes da correção.

Ainda sem decisão, fora da tabela: o cálculo sem gravar
(`POST /api/todogreen/calculate` e `simulate` sem `persist`) roda com os
parâmetros de preço do espaço para qualquer papel da vertical, embora ler esses
parâmetros exija `pricing:simulate`, `pricing:manage` ou o papel de auditor.
Nenhuma tela chama esses dois endpoints hoje.

## Defeitos

Comportamento errado que não é contorno de controle, mas quebra um processo.

| Código | Defeito | Onde | Situação |
| --- | --- | --- | --- |
| **D1** | O número da fatura se repete, e o segundo fechamento falha. `closeBilling` reserva o número com o tipo do documento (`cte`, `nfse` ou `nfe`), que o CHECK de `todogreen_document_series` (0053) não aceita — a própria 0069 registra isso; o `INSERT OR IGNORE` descarta a linha em silêncio e a reserva cai em 1. Toda fatura sai `CTE-000001`, e o segundo fechamento do mesmo tipo no mesmo espaço viola o índice único de `todogreen_invoices` (0056). | `todogreen-transactions.js` | **Aberto.** Reproduzido: o primeiro fechamento devolveu 201 com `CTE-000001`; o segundo, no mesmo espaço, 500 (`UNIQUE constraint failed: todogreen_invoices…`), e a série do CT-e nunca foi criada. Os testes fecham um faturamento só por espaço, por isso não pegam. A correção pede migração nova ou outro tipo de série, e teste com dois fechamentos. |

## Achados de leitura (ainda não reproduzidos)

Vistos na leitura do código nesta revisão e **ainda não reproduzidos** por
requisição — por isso fora das tabelas de lacunas e de defeitos. Cada um entra
numa delas (ou sai daqui) depois de um teste que o reproduza.

| Código | Achado | Onde |
| --- | --- | --- |
| **O1** | Deal Desk opcional: sem pedido aberto, a proposta sai mesmo com `approval.required` no cenário — nenhuma trava lê esse sinal, e abrir o pedido é ação manual na tela. | `dealDeskDomain.js`, `vertical-records/gates.js` |
| **O2** | Jurídico legado: um fluxo `legal` do painel empresarial com só a etapa `juridico` aprovada conclui o gate do contrato, e essa etapa é decidida por quem tem `deal:review` (pricing, produtos, planejamento, financeiro, operações e sustentabilidade), não `compliance:manage`. | `vertical-records/gates.js`, `todogreen-enterprise-workflows.js` |
| **O3** | Nenhuma tela montada cria o anexo que o gate de assinatura lê: o upload com contexto jurídico está em `pages/LegalPage.jsx`, que não é montado; o de contexto `workflow`, no painel empresarial, também não montado; o cofre envia sem contexto; e a Central Jurídica só guarda um link no evento, que o gate não lê. | `vertical-records/gates.js`, `vertical-records/eventos.js` |
| **O4** | Dupla baixa da compra: a recusa de baixa pelo razão só vale para a receita da ponte; o lançamento da compra pode ser baixado no razão e depois no título `payable-`, e o gatilho da 0069 soma o valor de novo. As duas telas oferecem a baixa. | `vertical-records/pagamentos.js`, `todogreen-transactions.js` |
| **O5** | CNPJ alfanumérico incompleto: as letras ainda são removidas no casamento do cliente pelo projetor TRACK3R, no cadastro "Dados da empresa", na chave digitada no registro manual do fiscal e na que vem do conector, na chave da NF-e do recebimento de compra e no DACTE — contra a regra do `AGENTS.md`. | `todogreen-track3r-projectors.js`, `todogreen-master-data.js`, `todogreen-fiscal.js`, `fiscalDomain.js`, `todogreen-purchasing.js`, `pages/FiscalPage.jsx` |
| **O6** | Mapas pela tela: sem token no navegador, a tela chama direto o Nominatim público e o OSRM de demonstração, e o botão de distância da Precificação nunca envia o cabeçalho de sessão — uso que o `AGENTS.md` proíbe. | `distanciaRodoviariaDomain.js`, `journeys/PricingPanel.jsx` |
| **O7** | Sessão de recarga marcada como OCPP pelo corpo: a fonte `ocpp` é aceita do cliente sem integração que a confirme. | `chargingSessionDomain.js`, `vertical-records/colecoes/energia.js` |
| **O8** | Recebimento de compra não idempotente: reenviar o POST de um recebimento parcial grava outro; a devolução sai do estoque sem a conferência de saldo do serviço de estoque. | `todogreen-purchasing.js` × `todogreen-stock.js` |
| **O9** | SOC lido como 0 vira 100 no cálculo das sugestões do pré-flight (`|| 100`); o check de energia em si preserva o 0. | `preflightDomain.js` |

## Jornada order-to-cash

O teste transversal `todogreen-erp-journey.worker.test.js` ("atravessa
Comercial, Jurídico, Implantação, Operação, Fiscal e Financeiro pela API")
percorre, num único caso, o caminho correto pelas APIs de negócio — e é a fonte
de regressão da jornada. A numeração abaixo é a dos comentários do próprio
teste:

1. conta / CRM (cliente, carteira e acesso ao portal) · 2. oportunidade ·
3. simulação persistida (pelo `/simulate`) · 4. Deal Desk com outro decisor ·
4b. snapshot de viabilidade · 5. proposta enviada e aceita · 6. tabela de preço
· 7. contrato (409 antes do Jurídico) · 8. workflow Jurídico pelo
`enterprise-workflows` (409 da assinatura sem anexo; o anexo é inserido por SQL)
· 9. contrato aprovado e assinado · 10. metodologia ESG · 11. operação (OS 409
antes do go-live; tabela inativa reprova) · 12. implantação (preparar,
configurar e ativar) · 13. OS com preço herdado · 14. cadastro de motorista e
veículo, despacho com pré-flight PASS · 15. vistoria (sem vistoria ou reprovada,
409; aprovada) e início da jornada · 16. coleta, trânsito e ocorrência ·
17. entrega com POD, visão do Portal do Cliente e fim da jornada ·
18. faturamento · 19. CT-e preparado internamente · 20. título, baixa e razão
quitado.

**Gates internos cobertos pela jornada:** Jurídico → contrato (409 antes do
Jurídico); assinatura → evidência (409 sem anexo); implantação ativa → OS;
tabela de preço → go-live. Os quatro estão implementados e testados, mas o do
Jurídico e o da assinatura têm lacuna (L1, L2, O2, O3), e a jornada passa pelo
ramo **legado** dos dois — o fluxo empresarial e o anexo de contexto
`workflow` —, não pelo registro canônico `todogreen_legal_records`.

**Um faturamento só:** a jornada fecha uma fatura por espaço, por isso não pega
o [D1](#defeitos).

**Fronteira externa:** CT-e autorizado só depois do retorno oficial da SEFAZ —
é o único `it.todo` do repositório,
`todogreen-erp-journey.worker.test.js` ("CT-e só pode aparecer como autorizado depois do retorno oficial da SEFAZ").

**Homologação:** não registrada. O `docs/HOMOLOGACAO_GO_LIVE.md` lista como
pendente "homologar com dados/volume reais em produção".

## Jornadas internas

| Jornada | Implementado | Testado | Homologado | Observação |
| --- | --- | --- | --- | --- |
| Procure-to-pay (requisição → alçada → pedido → recebimento → estoque → conta a pagar) | Parcial (L3, L8, L9, L10) | Sim | Não registrado | Toda etapa tem caso de worker, em casos separados: `todogreen-purchasing.worker.test.js` ("segue o caminho declarado e recusa pulo de etapa", "recusa gerar pedido de requisição não aprovada", "grava o total aprovado como retrato", "pedido enviado não tem os itens alterados", "entra no estoque e gera o título a pagar com vencimento pelo prazo"), `todogreen-purchasing-params.worker.test.js` ("PUT grava uma régua válida, versiona e persiste"), `todogreen-stock.worker.test.js` ("responde 409 com o saldo disponível e não grava nada") e `todogreen-vertical-records.worker.test.js` ("registra baixas parciais com histórico e impede pagamento acima do saldo"). Não há caso transversal, e a conciliação bancária do pagamento só tem teste negativo: `todogreen-treasury.worker.test.js` ("entrada no banco não concilia com custo"). Liquidação bancária é externa. |
| Hire-to-pay (colaborador → folha/férias/rescisão → fechamento → contas a pagar) | Parcial (tabelas de 2025; L3) | Sim | Não registrado | `todogreen-payroll.worker.test.js` ("cria colaborador válido e recusa CPF inválido", "fecha calculando o holerite e não fecha de novo", "fechar gera as quatro contas a pagar no razão, com vencimento no mês seguinte", "paga as férias (proporcional + 1/3), grava o holerite e lança no razão", "calcula as verbas, desliga o colaborador e lança no razão"). Rescisão só sem justa causa; eSocial, FGTS Digital e banco são externos. |
| Jurídico / Qualidade / Marketing (abertura → aprovação sequencial → histórico → conclusão → recorrência) | Parcial (L1, L6) | Parcial | Não registrado | Só o Jurídico tem teste de fluxo: `todogreen-enterprise-workflows.worker.test.js` ("exige a nota, registra a ressalva e faz o fluxo avançar sem reprovar", "reprovar continua encerrando o processo") e o passo 8 da jornada. A recorrência de um processo geral é conferida por contagem em `cron.worker.test.js` ("o disparo horário roda os jobs horários e o rastreador uma vez só"). Qualidade e Marketing não têm teste de fluxo. |

## Prioridades resultantes

- **P0 — D1 e L8, antes de tudo.** O faturamento quebra na segunda fatura do
  mesmo tipo no espaço, e qualquer papel da vertical — inclusive o colaborador —
  lê tesouraria, títulos, compras e estoque. Cada um com teste que reproduza.
- **P0 — lacunas de controle abertas (L1, L2, L3, L6, L9, L10, L11, L12):** cada
  uma em PR próprio, com teste que reproduza o contorno. L4, L5 e L7 foram
  corrigidas neste PR.
- **P0 — pré-flight no despacho automático: fechado.** `/dispatch/aplicar`
  registra o pré-flight, aplica a guarda canônica da rota, recusa BLOCK e exige
  justificativa em WARNING (`todogreen-dispatch.worker.test.js`).
- **P0 — produção só publica após gate de navegador: parcial.** `test:e2e:critical`
  é gate antes do merge e no fallback manual `deploy.yml`; dentro do Workers
  Builds ele não roda (o container não instala Chromium). Os E2E críticos
  conferem que as telas e os portais abrem com a sessão real, não efeitos de
  negócio.
- **P1 — reproduzir os achados de leitura (O1–O9)** e movê-los para a tabela
  certa, ou tirá-los daqui.
- **P1 — publicação automática da `main`:** em 24/09 a produção veio de
  publicação manual a partir de um ramo local e ficou horas atrás da `main`;
  às 23:53 UTC alcançou a `main`, mas pelo botão manual do GitHub Actions
  (`publishedBy: github-actions`). Depois de um merge, `/api/system/version`
  deve responder o `sha` da `main` com `publishedBy: cloudflare-workers-builds`
  sem ninguém apertar botão; conferir a configuração do Workers Builds
  (`docs/CLOUDFLARE_BUILDS_SETUP.md`).
- **P1 — proteger a `main`** por configuração do GitHub
  (`docs/GITHUB_MAIN_PROTECTION.md`, `scripts/github/protect-main.sh`); não é
  verificável pelo repositório.
- **P1 — homologar com dado real** e registrar na tabela de homologação: TRACK3R
  (evento real), importação de arquivo real, portal do cliente, e-mail no Worker
  `allgreen`.
- **P1 — tabelas da folha de 2026** e sua configuração fora do código.
- **P2 — capacidade:** o harness k6 (`scripts/load/todogreen-smoke.k6.js`) existe,
  mas não há resultado registrado em `docs/LOAD_TEST.md`.

## Divergências corrigidas nesta revisão

**Primeira passada** — a versão anterior afirmava, e o código não confirma:

1. "Workflow Jurídico — ainda não é hard gate do contrato": é gate
   (`juridicoConcluido`), e a própria matriz dizia isso em outra linha.
2. "Jurídico no ciclo contratual — nenhum atalho interno" e "os quatro atalhos
   foram fechados": ver L1 e L2.
3. "Provedor de assinatura — estrutura de readiness existe": não há.
4. "Alçada de compras — faixas ainda são codificadas": já são configuráveis por
   espaço (0111), sem histórico de versões.
5. "Cotação / fornecedor — comparação de ofertas": não está no ERP.
6. "monday.com — sem conector backend ativo": o conector existe; falta a projeção.
7. "Seleção de motor — alternativas de rota ainda não conectadas": estão, fora do
   caminho Geoapify.
8. "Não há vertical Greenmob": há, só de frontend.
9. "Planejamento / aceite — capacidade, produto, risco e decisão persistidos":
   só a OS é persistida.
10. "Fechamento de competência — nenhum gap": ver L3.
11. "Folha — tabelas versionadas": só existe a tabela de 2025, no código.
12. "Webhooks de saída" REAL para o ERP: nenhum evento `todogreen_*` dispara.
13. "Negociação contratual — falta amarração com o status final": o status final
    já depende do Jurídico; o que falta é amarrar os pontos de negociação.
14. Qualidade, Marketing e processos gerais com "aprovação sequencial" e "motor
    REAL": a aprovação é contornável (L6) e só o Jurídico tem teste.
15. E-mail transacional como EXTERNO: está implementado e em uso.
16. Gmail/Agenda como só EXTERNO: há envio real pelo navegador.
17. O cabeçalho da seção de eletrificação dizia PARCIAL e que o snapshot de
    viabilidade ainda precisava de D1 e gate — ambos existem.
18. Contagens de testes citadas não batiam (ex.: 19 × 18 na tarifa ANEEL; 13 × 11
    no snapshot) — por isso a matriz deixou de citar contagem.
19. Caminhos errados: `design-system/…` fica em `src/design-system/…`.
20. Módulos inteiros ausentes — ver [Módulos que não estavam na matriz](#módulos-que-não-estavam-na-matriz).

**Segunda passada** — reconferência linha a linha, com as citações conferidas
por teste:

21. Precificação com "motor no servidor": nenhuma tela chama o motor; o cenário
    vem pronto do navegador (L11).
22. Deal Desk e Proposta como "Sim": o pedido é opcional (O1) e o cenário não é
    conferido (L12).
23. Portal do cliente como "Sim": a própria linha declarava a lacuna das
    solicitações.
24. Faturamento como "Sim": o segundo fechamento falha (D1).
25. NFS-e como "Preparado": não há código dormente de NFS-e.
26. Pedido de compra "editar derruba a aprovação": só marca um indicador (L9);
    Recebimento "idempotente": é imutável, não idempotente (O8).
27. Folha "a reabertura não tem teste": tem; RH operacional com "escalas
    (0110)": a 0110 é o turno do motorista, e não há escala.
28. Processos internos gerais e Jornadas internas "a recorrência não tem
    teste": tem, em `cron.worker.test.js`.
29. Frota com "eventos (0031)": a tabela de eventos não é usada.
30. Impacto ambiental "(0103/0108)": o cálculo fica na 0027; a 0103 é a régua,
    e a 0108, o fechamento.
31. Radar por busca web "(0075)": `todogreen-market-radar.js` não grava; os
    acompanhamentos ficam na 0052.
32. "Sobre o negócio" com "0091–0095": a 0094 é a idempotência do ledger.
33. GreenPay "dormente sem o token": o repasse pede token **e** URL.
34. Core All Green "Green On sem backend nem D1": os pontos, sessões e preços
    de recarga são D1.
35. CIOT "piso mínimo": só o informado. E-mail transacional e WhatsApp como
    "Testado: Sim": o envio real cai no mock.
36. Restrições viárias como "Parcial": sem efeito no servidor nem tela, é "Não"
    pela definição do topo.
37. Registro de homologação: H2 citava um deploy de prévia; H3 tinha a data
    errada; H5 citava uma frase que saiu do `AGENTS.md`; H6 não era registro de
    validação (retirado); H7 omitia o ambiente anterior.
38. Nomes de `describe` citados como se fossem casos, e casos com o nome cortado
    (ex.: "…(reserva atômica)", "…(ponte 0069)").
39. Numeração da jornada: a matriz listava 22 passos; o teste numera de 1 a 20,
    com 4b.
40. `docs/SECRETS.md` dizia que, sem `TDG_OSRM_BASE_URL`, o OSRM não é
    oferecido; o código sempre inclui o OSRM público.

## Como manter esta matriz

- Mudou o comportamento de um processo? Atualize a linha **no mesmo PR**.
- **Implementado** e **Testado** se provam com código e teste do repositório;
  **Homologado** só com linha nova no [registro de homologação](#registro-de-homologação-em-produção).
- Cite o arquivo de teste e o nome exato do caso, assim:
  `todogreen-stock.worker.test.js` ("responde 409 com o saldo disponível e não grava nada").
  Dentro dos parênteses vão só nomes entre aspas; comentário vai fora deles.
  Nunca contagem de testes nem número de linha — os dois envelhecem calados.
- Esta matriz é conferida por `src/readiness-matrix.test.js`: cada coluna usa
  só o vocabulário do topo; "Homologado" cita código que existe no registro;
  "Testado: Sim/Parcial" cita pelo menos um teste que roda no gate; cada caso
  citado existe, literalmente, no arquivo; e cada migração citada existe em
  `migrations/`.
- Os caminhos citados nos documentos são conferidos por
  `src/docs-references.test.js`: arquivo renomeado ou removido reprova o teste
  até a documentação acompanhar.
